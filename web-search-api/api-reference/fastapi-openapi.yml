openapi: 3.1.0
info:
  title: NewsCatcher CatchAll API
  version: 1.7.0
  contact:
    name: NewsCatcher
    url: https://newscatcherapi.com
    email: support@newscatcherapi.com
  description: 'CatchAll is a web search API that generates unique datasets that don''t
    exist anywhere else on the web. Built on NewsCatcher''s proprietary real-world
    event index, it delivers state-of-the-art recall—finding all relevant events,
    not just top results.


    ### Authentication


    All endpoints except /health and /version require `x-api-key` header. If the key
    is invalid or missing, the API returns the `403 Forbidden` error.


    ### Job workflow


    1. (Optional) Get suggestions via /catchAll/initialize

    2. Submit a query via /catchAll/submit with optional date ranges and custom validators/enrichments

    3. Poll /catchAll/status/{job_id} until completed (10-15 minutes)

    4. Retrieve results via /catchAll/pull/{job_id}


    ### Monitor workflow


    1. Create successful job via /catchAll/submit

    2. Create monitor via /catchAll/monitors/create with schedule

    3. Retrieve aggregated results via /catchAll/monitors/pull/{monitor_id}


    ### Company search workflow


    1. Create a dataset via `POST /catchAll/datasets/` or `POST /catchAll/datasets/upload`

    2. Wait for the dataset `latest_status` to reach `ready`

    3. Submit a job with `connected_dataset_ids` pointing to your dataset

    4. Retrieve results — each record includes a `connected_entities` array with relevance
    scores per matched company


    ### Important notes


    **Dynamic schemas**: Response schemas are generated dynamically by LLMs. Field
    names in the `enrichment` object may vary and are not deterministic across jobs
    unless explicitly specified.'
paths:
  /catchAll/validate:
    post:
      tags:
      - Jobs
      summary: Check Query Quality
      description: 'Use this endpoint before submitting a job to validate that your
        query

        is well-formed and likely to produce good results.'
      operationId: validateQuery
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CheckQueryQualityRequestDto'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CheckQueryQualityResponseDto'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/submit:
    post:
      tags:
      - Jobs
      summary: Create a new job
      description: 'Submits a natural-language query to the CatchAll pipeline and
        returns a `job_id` immediately. The job runs asynchronously.


        **Workflow:** submit → poll `/status/{job_id}` every 5 s → pull results from
        `/pull/{job_id}`.


        **Concurrency:** the `Jobs_Concurrency` plan entitlement limits simultaneous
        in-flight jobs. A 429 is returned if the limit is reached.'
      operationId: createJob
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubmitRequestDto'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubmitResponseDto'
        '400':
          description: Validation error (invalid query, bad dates, empty validators
            list, etc.)
        '403':
          description: Invalid or missing API key, or unauthorized webhook/dataset
            access
        '404':
          description: Webhook ID or project not found
        '429':
          description: Concurrency limit reached
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
      externalDocs:
        description: Detailed documentation for job creation
        url: https://www.newscatcherapi.com/docs/web-search-api/api-reference/jobs/create-job
  /catchAll/initialize:
    post:
      tags:
      - Jobs
      summary: Preview suggested validators and enrichments
      description: 'Dry-run endpoint that returns the validators and enrichments the
        API would auto-generate for a given query. Use this before `/submit` to decide
        which fields to pass explicitly.


        Does **not** create a job or consume any quota.'
      operationId: initialize
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SuggestInitializeRequestDto'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuggestInitializeResponseDto'
        '400':
          description: Validation error
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
      externalDocs:
        description: Learn about validator and enrichment suggestions
        url: https://www.newscatcherapi.com/docs/web-search-api/api-reference/jobs/initialize-job
  /catchAll/status/{job_id}:
    get:
      tags:
      - Jobs
      summary: Get job status
      description: 'Returns the current pipeline stage and step-level completion flags.


        **Recommended polling interval:** 5 seconds. Stop polling when `status` is
        `completed` or `failed`.


        **Stages in order:** submitted → analyzing → fetching → clustering → enriching
        → completed'
      operationId: getJobStatus
      parameters:
      - name: job_id
        in: path
        required: true
        schema:
          type: string
          title: Job Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatusResponseDto'
        '403':
          description: Job does not belong to this user
        '404':
          description: Job not found
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
      externalDocs:
        description: Understanding job statuses and polling
        url: https://www.newscatcherapi.com/docs/web-search-api/api-reference/jobs/get-job-status
  /catchAll/pull/{job_id}:
    get:
      tags:
      - Jobs
      summary: Pull job results
      description: 'Returns paginated result records for a job.


        You may call this at any pipeline stage — `all_records` will be partially
        populated while the job is still running. Use `page` and `page_size` to paginate
        through results.'
      operationId: getJobResults
      parameters:
      - name: job_id
        in: path
        required: true
        schema:
          type: string
          title: Job Id
      - name: page
        in: query
        required: false
        schema:
          type: integer
          default: 1
          title: Page
      - name: page_size
        in: query
        required: false
        schema:
          type: integer
          default: 100
          title: Page Size
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PullResponseDto'
        '403':
          description: Job does not belong to this user
        '404':
          description: Job not found
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
      externalDocs:
        description: Working with job results and dynamic schemas
        url: https://www.newscatcherapi.com/docs/web-search-api/api-reference/jobs/get-job-results
  /catchAll/pull/{job_id}/csv:
    get:
      tags:
      - Jobs
      summary: Pull job results as CSV
      description: Returns job result records as a CSV download. One row per record
        with enrichment fields as columns and citations as a JSON column. If entity
        datasets were used, connected entities are split into event_associated_entities
        and mention_entities JSON columns; otherwise those columns are omitted.
      operationId: getJobResultsCsv
      parameters:
      - name: job_id
        in: path
        required: true
        schema:
          type: string
          title: Job Id
      responses:
        '200':
          description: CSV export
          content:
            application/json:
              schema: {}
            text/csv: {}
        '403':
          description: Job does not belong to this user
        '404':
          description: Job not found
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/jobs/user:
    get:
      tags:
      - Jobs
      summary: List jobs for the authenticated API key
      description: Returns a paginated list of jobs owned by or shared with the caller.
        API key values are masked in the response.
      operationId: getUserJobs
      parameters:
      - name: page
        in: query
        required: false
        schema:
          type: integer
          default: 1
          title: Page
      - name: page_size
        in: query
        required: false
        schema:
          type: integer
          default: 100
          title: Page Size
      - name: search
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Search
      - name: ownership
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Ownership'
          default: all
      - name: project_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Project Id
      - name: mode
        in: query
        required: false
        schema:
          anyOf:
          - $ref: '#/components/schemas/ProcessingMode'
          - type: 'null'
          description: Filter jobs by processing mode.
          title: Mode
        description: Filter jobs by processing mode.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListUserJobsResponseDto'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
      externalDocs:
        description: Learn about job management
        url: https://www.newscatcherapi.com/docs/web-search-api/api-reference/jobs/list-user-jobs
  /catchAll/jobs/{job_id}:
    delete:
      tags:
      - Jobs
      summary: Delete a job
      description: Soft-deletes a job and its associated results.
      operationId: deleteJob
      parameters:
      - name: job_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Job Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteJobResponseDto'
        '403':
          description: Job does not belong to this user
        '404':
          description: Job not found
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/continue:
    post:
      tags:
      - Jobs
      summary: Continue a job to fetch more results
      description: Use this after reviewing a completed job's results and deciding
        you want more. Increases the job's `limit` and resumes fetching — the existing
        results are kept and new ones are appended. The `Jobs_Concurrency` plan entitlement
        is re-checked; a 429 is returned if it is already exhausted.
      operationId: continueJob
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContinueRequestDto'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContinueResponseDto'
        '400':
          description: Validation error or new_limit <= current limit
        '403':
          description: Job does not belong to this user
        '429':
          description: Concurrency limit reached
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
      externalDocs:
        description: Learn about job continuation and limits
        url: https://www.newscatcherapi.com/docs/web-search-api/api-reference/jobs/continue-job
  /catchAll/user/limits:
    post:
      tags:
      - Meta
      summary: Get plan limits
      description: Returns plan features and current usage for the authenticated organization.
      operationId: getPlanLimits
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetFeaturesResponse'
        '400':
          description: Invalid organization ID or subscription ID format.
        '404':
          description: Subscription or user not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/monitors/create:
    post:
      tags:
      - Monitors
      summary: Create a monitor
      description: 'Creates a scheduled monitor from a reference completed job. The
        monitor re-runs the reference job''s query/validators/enrichments on a cron
        schedule.


        The `Monitor_Active_Slots` plan entitlement is reserved on creation. Use `/disable`
        to pause without losing the slot.


        **Schedule examples:** `''every day at 9 AM''`, `''every Monday at 6 PM EST''`.'
      operationId: createMonitor
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateMonitorRequestDto'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateMonitorResponseDto'
        '400':
          description: Validation error (invalid schedule, timezone, limit, or webhook
            limit exceeded)
        '403':
          description: Invalid or missing API key
        '404':
          description: Reference job or webhook not found
        '422':
          description: Schedule could not be parsed into a cron expression
        '429':
          description: Monitor active slots limit reached
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/monitors/{monitor_id}/status:
    get:
      tags:
      - Monitors
      summary: Get monitor status history
      description: Returns all status entries for a monitor, ordered newest-first.
        Each entry has a status type, timestamp, and additional_information dict.
      operationId: getMonitorStatusHistory
      parameters:
      - name: monitor_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Monitor Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MonitorStatusHistoryResponseDto'
        '403':
          description: Monitor does not belong to this user
        '404':
          description: Monitor not found or no status entries yet
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/monitors/{monitor_id}/jobs:
    get:
      tags:
      - Monitors
      summary: List jobs spawned by a monitor
      description: Returns all jobs created by past monitor runs, sorted by start_date.
        Each entry includes job_id, start_date, and end_date.
      operationId: listMonitorJobs
      parameters:
      - name: monitor_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Monitor Id
      - name: sort
        in: query
        required: false
        schema:
          type: string
          pattern: ^(asc|desc)$
          description: Sort by start_date (asc or desc)
          default: asc
          title: Sort
        description: Sort by start_date (asc or desc)
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListMonitorJobsResponseDto'
        '403':
          description: Monitor does not belong to this user
        '404':
          description: No jobs found for this monitor
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/monitors/pull/{monitor_id}:
    get:
      tags:
      - Monitors
      summary: Pull latest results for a monitor
      description: Returns the most recent run's aggregated records. Structure mirrors
        job pull with the addition of cron_expression and reference_job.
      operationId: pullMonitorResults
      parameters:
      - name: monitor_id
        in: path
        required: true
        schema:
          type: string
          title: Monitor Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PullMonitorResponseDto'
        '403':
          description: Monitor does not belong to this user
        '404':
          description: Monitor not found or no results yet
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/monitors/pull/{monitor_id}/csv:
    get:
      tags:
      - Monitors
      summary: Pull latest monitor results as CSV
      description: Returns the most recent run's records as a CSV download. One row
        per record with enrichment fields as columns and citations as a JSON column.
        If entity datasets were used, connected entities are split into event_associated_entities
        and mention_entities JSON columns; otherwise those columns are omitted.
      operationId: pullMonitorResultsCsv
      parameters:
      - name: monitor_id
        in: path
        required: true
        schema:
          type: string
          title: Monitor Id
      responses:
        '200':
          description: CSV export
          content:
            application/json:
              schema: {}
            text/csv: {}
        '403':
          description: Monitor does not belong to this user
        '404':
          description: Monitor not found or no results yet
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/monitors/{monitor_id}/disable:
    post:
      tags:
      - Monitors
      summary: Disable a monitor
      description: Pauses future runs without deleting the monitor or releasing the
        entitlement slot.
      operationId: disableMonitor
      parameters:
      - name: monitor_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Monitor Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DisableMonitorResponseDto'
        '403':
          description: Monitor does not belong to this user
        '404':
          description: Monitor not found
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/monitors/{monitor_id}/enable:
    post:
      tags:
      - Monitors
      summary: Enable (re-activate) a monitor
      description: 'Re-activates a disabled monitor. The Monitor_Active_Slots entitlement
        is re-reserved.


        Optional body: `{ "backfill": true }` (default). When backfill=true the first
        run chains forward from the last job''s end date.'
      operationId: enableMonitor
      parameters:
      - name: monitor_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Monitor Id
      requestBody:
        content:
          application/json:
            schema:
              anyOf:
              - $ref: '#/components/schemas/EnableMonitorRequestDto'
              - type: 'null'
              title: Body
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EnableMonitorResponseDto'
        '403':
          description: Monitor does not belong to this user
        '404':
          description: Monitor not found
        '429':
          description: Monitor active slots limit reached
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/monitors/{monitor_id}:
    patch:
      tags:
      - Monitors
      summary: Update webhook assignments or limit
      description: 'Updates the webhook assignments and/or result limit for a monitor.
        Pass an empty `webhook_ids: []` to clear all webhook assignments.'
      operationId: updateMonitor
      parameters:
      - name: monitor_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Monitor Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateMonitorRequestDto'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateMonitorResponseDto'
        '400':
          description: Validation error or webhook limit exceeded (max 5 per resource)
        '403':
          description: Monitor does not belong to this user
        '404':
          description: Monitor or webhook not found
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
      externalDocs:
        description: Learn about monitor configuration
        url: https://www.newscatcherapi.com/docs/web-search-api/how-to/configure-monitors
    delete:
      tags:
      - Monitors
      summary: Delete a monitor
      description: Soft-deletes a monitor and releases the Monitor_Active_Slots entitlement.
      operationId: deleteMonitor
      parameters:
      - name: monitor_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Monitor Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteMonitorResponseDto'
        '403':
          description: Monitor does not belong to this user
        '404':
          description: Monitor not found
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/monitors:
    get:
      tags:
      - Monitors
      summary: List monitors for the authenticated API key
      description: Returns monitors owned by or shared with the caller. API key values
        are masked.
      operationId: listMonitors
      parameters:
      - name: page
        in: query
        required: false
        schema:
          type: integer
          default: 1
          title: Page
      - name: page_size
        in: query
        required: false
        schema:
          type: integer
          default: 100
          title: Page Size
      - name: search
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Search
      - name: ownership
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Ownership'
          default: all
      - name: project_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Project Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListMonitorsResponseDto'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/entities:
    post:
      tags:
      - Entities
      summary: Create entity
      description: Creates a single company entity for the organization. Entities
        are the atomic units used in datasets and job targeting. Returns the new entity's
        ID and initial processing status. In addition to name, the entity must include
        at least one of description or additional_attributes.company_attributes.domain.
      operationId: createEntity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateEntityRequestDto'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateEntityResponseDto'
        '422':
          description: 'Validation error, including: neither description nor additional_attributes.company_attributes.domain
            was provided.'
        '401':
          description: Missing or invalid authentication credentials.
    get:
      tags:
      - Entities
      summary: List entities
      description: Returns a paginated list of entities for the organization. Filter
        by status, entity_type, or search by name. Supports sorting.
      operationId: listEntities
      parameters:
      - name: page
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          default: 1
          title: Page
      - name: page_size
        in: query
        required: false
        schema:
          type: integer
          maximum: 500
          minimum: 1
          default: 100
          title: Page Size
      - name: search
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Search
      - name: status
        in: query
        required: false
        schema:
          anyOf:
          - $ref: '#/components/schemas/EntityStatus'
          - type: 'null'
          title: Status
      - name: entity_type
        in: query
        required: false
        schema:
          anyOf:
          - $ref: '#/components/schemas/EntityType'
          - type: 'null'
          title: Entity Type
      - name: sort_by
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/EntitySortBy'
          default: created_at
      - name: sort_order
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/SortOrder'
          default: desc
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EntityListResponseDto'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/entities/batch:
    post:
      tags:
      - Entities
      summary: Batch create entities
      description: Creates multiple entities in one call. Each entity in the array
        is processed independently. Returns the IDs and statuses for all created entities.
        Useful for bulk imports. Each entity must include at least one of description
        or additional_attributes.company_attributes.domain, in addition to name.
      operationId: createEntitiesBatch
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateEntitiesBatchRequestDto'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateEntitiesBatchResponseDto'
        '422':
          description: 'Validation error on one or more entities, including: neither
            description nor additional_attributes.company_attributes.domain was provided.'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/entities/{entity_id}:
    get:
      tags:
      - Entities
      summary: Get entity
      description: Returns full details of a single entity by ID, including its attributes
        and current processing status.
      operationId: getEntity
      parameters:
      - name: entity_id
        in: path
        required: true
        schema:
          type: string
          title: Entity Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EntityResponseDto'
        '403':
          description: Entity does not belong to this user.
        '404':
          description: Entity not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
    patch:
      tags:
      - Entities
      summary: Update entity
      description: Updates name, description, or additional_attributes of an entity.
        Only supplied fields are changed. Returns the updated entity.
      operationId: updateEntity
      parameters:
      - name: entity_id
        in: path
        required: true
        schema:
          type: string
          title: Entity Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateEntityRequestDto'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EntityResponseDto'
        '403':
          description: Entity does not belong to this user.
        '404':
          description: Entity not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
    delete:
      tags:
      - Entities
      summary: Delete entity
      description: Permanently deletes an entity. If the entity is currently part
        of a dataset, it is removed from all dataset memberships. Returns 204 No Content.
      operationId: deleteEntity
      parameters:
      - name: entity_id
        in: path
        required: true
        schema:
          type: string
          title: Entity Id
      responses:
        '204':
          description: Successful Response
        '403':
          description: Entity does not belong to this user.
        '404':
          description: Entity not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/datasets:
    post:
      tags:
      - Datasets
      summary: Create dataset
      description: Creates a named dataset and optionally pre-populates it with entity
        IDs. A dataset is a curated collection of companies used to scope job searches.
      operationId: createDataset
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateDatasetRequestDto'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DatasetResponseDto'
        '400':
          description: One or more entity IDs not found.
        '403':
          description: Not authorized.
        '404':
          description: Resource not found.
        '409':
          description: A dataset with this name already exists in the organization.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
    get:
      tags:
      - Datasets
      summary: List datasets
      description: Returns a paginated, sortable list of datasets for the organization.
        Filter by status, ownership, or search by name. Use project_id to scope to
        a project.
      operationId: listDatasets
      parameters:
      - name: page
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          default: 1
          title: Page
      - name: page_size
        in: query
        required: false
        schema:
          type: integer
          maximum: 500
          minimum: 1
          default: 100
          title: Page Size
      - name: search
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Search
      - name: latest_status
        in: query
        required: false
        schema:
          anyOf:
          - $ref: '#/components/schemas/DatasetStatus'
          - type: 'null'
          title: Latest Status
      - name: sort_by
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/DatasetSortBy'
          default: created_at
      - name: sort_order
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/SortOrder'
          default: desc
      - name: ownership
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Ownership'
          default: all
      - name: project_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Project Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DatasetListResponseDto'
        '403':
          description: Not authorized.
        '404':
          description: Resource not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/datasets/{dataset_id}:
    get:
      tags:
      - Datasets
      summary: Get dataset
      description: Returns the full detail of a single dataset by ID, including its
        entity count and latest status.
      operationId: getDataset
      parameters:
      - name: dataset_id
        in: path
        required: true
        schema:
          type: string
          title: Dataset Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DatasetResponseDto'
        '403':
          description: Dataset does not belong to this user.
        '404':
          description: Dataset not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
    patch:
      tags:
      - Datasets
      summary: Update dataset
      description: Updates the name or description of a dataset. Only supplied fields
        are changed.
      operationId: updateDataset
      parameters:
      - name: dataset_id
        in: path
        required: true
        schema:
          type: string
          title: Dataset Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateDatasetRequestDto'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DatasetResponseDto'
        '403':
          description: Dataset does not belong to this user.
        '404':
          description: Dataset not found.
        '409':
          description: A dataset with this name already exists in the organization.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
    delete:
      tags:
      - Datasets
      summary: Delete dataset
      description: Permanently deletes a dataset and its entity associations. Returns
        204 No Content.
      operationId: deleteDataset
      parameters:
      - name: dataset_id
        in: path
        required: true
        schema:
          type: string
          title: Dataset Id
      responses:
        '204':
          description: Successful Response
        '403':
          description: Dataset does not belong to this user.
        '404':
          description: Dataset not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/datasets/{dataset_id}/entities:
    post:
      tags:
      - Datasets
      summary: Add entities to dataset
      description: Adds one or more entities to a dataset by their IDs. Duplicate
        additions are silently ignored. Returns the number of newly added entities.
      operationId: addEntitiesToDataset
      parameters:
      - name: dataset_id
        in: path
        required: true
        schema:
          type: string
          title: Dataset Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DatasetEntityIdsRequestDto'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ManageEntitiesResponseDto'
        '403':
          description: Dataset does not belong to this user.
        '404':
          description: Dataset not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
    delete:
      tags:
      - Datasets
      summary: Remove entities from dataset
      description: Removes one or more entities from a dataset by their IDs. Returns
        the number of removed entities.
      operationId: removeEntitiesFromDataset
      parameters:
      - name: dataset_id
        in: path
        required: true
        schema:
          type: string
          title: Dataset Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DatasetEntityIdsRequestDto'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ManageEntitiesResponseDto'
        '403':
          description: Dataset does not belong to this user.
        '404':
          description: Dataset not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/datasets/{dataset_id}/entities/list:
    post:
      tags:
      - Datasets
      summary: List entities in dataset
      description: Returns a paginated, filterable list of entities belonging to this
        dataset. Filter by status, entity_type, or search by name. Supports sorting.
      operationId: listEntitiesInDataset
      parameters:
      - name: dataset_id
        in: path
        required: true
        schema:
          type: string
          title: Dataset Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ListDatasetEntitiesRequestDto'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DatasetEntityListResponseDto'
        '403':
          description: Dataset does not belong to this user.
        '404':
          description: Dataset not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/datasets/{dataset_id}/status:
    get:
      tags:
      - Datasets
      summary: Get dataset status history
      description: Returns the chronological status change history of a dataset (e.g.
        created → indexing → ready).
      operationId: getDatasetStatusHistory
      parameters:
      - name: dataset_id
        in: path
        required: true
        schema:
          type: string
          title: Dataset Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DatasetStatusHistoryResponseDto'
        '403':
          description: Dataset does not belong to this user.
        '404':
          description: Dataset not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/datasets/upload:
    post:
      tags:
      - Datasets
      summary: Create dataset from CSV upload
      description: Creates a new dataset by uploading a CSV file. The CSV must have
        at least a `name` column; additional columns are mapped to entity attributes.
        Max file size is plan-dependent. Returns the created dataset ID.
      operationId: createDatasetFromCsv
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Body_createDatasetFromCsv'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CsvUploadCreateResponseDto'
        '400':
          description: File is not a valid CSV.
        '409':
          description: A dataset with this name already exists in the organization.
        '422':
          description: CSV parsing error or missing required columns.
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/datasets/{dataset_id}/upload:
    post:
      tags:
      - Datasets
      summary: Append entities from CSV to existing dataset
      description: Parses a CSV file and appends the entities to an existing dataset.
        Duplicate rows by name are skipped.
      operationId: uploadCSVToDataset
      parameters:
      - name: dataset_id
        in: path
        required: true
        schema:
          type: string
          title: Dataset Id
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Body_uploadCSVToDataset'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CsvUploadToExistingResponseDto'
        '400':
          description: File is not a valid CSV.
        '403':
          description: Dataset does not belong to this user.
        '404':
          description: Dataset not found.
        '422':
          description: CSV parsing error.
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/projects:
    post:
      tags:
      - Projects
      summary: Create project
      description: Creates a new project for the organization. Projects are named
        containers used to group related jobs, monitors, datasets, monitor groups,
        and webhooks.
      operationId: createProject
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProjectRequestDto'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateProjectResponseDto'
        '422':
          description: Validation error.
        '401':
          description: Missing or invalid authentication credentials.
    get:
      tags:
      - Projects
      summary: List projects
      description: Returns a paginated list of projects in the organization. Use search
        to filter by name. Use ownership=mine to see only your own projects.
      operationId: listProjects
      parameters:
      - name: page
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          default: 1
          title: Page
      - name: page_size
        in: query
        required: false
        schema:
          type: integer
          maximum: 500
          minimum: 1
          default: 100
          title: Page Size
      - name: search
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Search
      - name: ownership
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/Ownership'
          default: all
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectListResponseDto'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/projects/{project_id}:
    get:
      tags:
      - Projects
      summary: Get project
      description: Returns the full detail of a project by ID.
      operationId: getProject
      parameters:
      - name: project_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Project Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetProjectResponseDto'
        '403':
          description: Project does not belong to the user.
        '404':
          description: Project not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
    patch:
      tags:
      - Projects
      summary: Update project
      description: Updates the name or description of a project. Only supplied fields
        are changed.
      operationId: updateProject
      parameters:
      - name: project_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Project Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProjectRequestDto'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateProjectResponseDto'
        '403':
          description: User does not have permission to update this project.
        '404':
          description: Project not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
    delete:
      tags:
      - Projects
      summary: Delete project
      description: 'Deletes a project. If delete_resources=true, all jobs, monitors,
        datasets, and monitor groups in the project are also deleted. Webhooks are
        handled differently: they are never deleted by this operation — any attached
        webhook is simply detached from the project and continues to exist and remain
        fully usable/shareable on its own, since a webhook can be attached to other
        resources independently of this project. If delete_resources=false, only the
        project container is deleted and all resources (including webhooks) become
        unassigned.'
      operationId: deleteProject
      parameters:
      - name: project_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Project Id
      - name: delete_resources
        in: query
        required: false
        schema:
          type: boolean
          default: false
          title: Delete Resources
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteProjectResponseDto'
        '403':
          description: User does not have permission to delete this project.
        '404':
          description: Project not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/projects/{project_id}/overview:
    get:
      tags:
      - Projects
      summary: Get project overview
      description: 'Returns a high-level summary of a project: total resource counts
        by type (jobs, monitors, datasets, monitor groups, webhooks).'
      operationId: getProjectOverview
      parameters:
      - name: project_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Project Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectOverviewResponseDto'
        '403':
          description: Project does not belong to the user.
        '404':
          description: Project not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/projects/{project_id}/resources:
    post:
      tags:
      - Projects
      summary: Add resources to project
      description: Attaches one or more existing resources (job, monitor, dataset,
        monitor_group, or webhook) to a project. Returns 201 when at least one resource
        is newly attached, 200 if all were already associated.
      operationId: addResourceToProject
      parameters:
      - name: project_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Project Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddResourceRequestDto'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AddResourceResponseDto'
        '403':
          description: Not authorized to modify this project.
        '404':
          description: Project not found.
        '422':
          description: Validation error.
        '401':
          description: Missing or invalid authentication credentials.
    get:
      tags:
      - Projects
      summary: List project resources
      description: Returns a paginated list of resources associated with this project.
        Filter by resource_type.
      operationId: listProjectResources
      parameters:
      - name: project_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Project Id
      - name: resource_type
        in: query
        required: false
        schema:
          anyOf:
          - $ref: '#/components/schemas/ProjectResourceTypeEnum'
          - type: 'null'
          title: Resource Type
      - name: page
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          default: 1
          title: Page
      - name: page_size
        in: query
        required: false
        schema:
          type: integer
          maximum: 500
          minimum: 1
          default: 100
          title: Page Size
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceListResponseDto'
        '403':
          description: Not authorized.
        '404':
          description: Project not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/projects/{project_id}/resources/{resource_type}/{resource_id}:
    delete:
      tags:
      - Projects
      summary: Remove resource from project
      description: Detaches a resource from a project without deleting the resource
        itself.
      operationId: removeResourceFromProject
      parameters:
      - name: project_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Project Id
      - name: resource_type
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/ProjectResourceTypeEnum'
      - name: resource_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Resource Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RemoveResourceResponseDto'
        '403':
          description: Not authorized to modify this project.
        '404':
          description: Project or resource not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/webhooks:
    post:
      tags:
      - Webhooks
      summary: Create webhook
      description: Creates a new webhook endpoint for the organization. Webhooks can
        be typed (slack, teams, generic) and configured with delivery_mode (full or
        per_record), custom headers, query params, and auth. Max webhook limit is
        plan-dependent. Optionally pass project_id to associate the webhook with a
        project immediately upon creation.
      operationId: createWebhook
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateWebhookRequestDto'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateWebhookResponseDto'
        '403':
          description: Project does not belong to the caller's organization.
        '404':
          description: project_id does not reference an existing project.
        '422':
          description: Plan limit reached or validation error.
        '401':
          description: Missing or invalid authentication credentials.
    get:
      tags:
      - Webhooks
      summary: List webhooks
      description: Returns a paginated list of webhooks belonging to the organization.
        Use page/page_size for pagination. Filter by name fragment with search.
      operationId: listWebhooks
      parameters:
      - name: page
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          default: 1
          title: Page
      - name: page_size
        in: query
        required: false
        schema:
          type: integer
          maximum: 500
          minimum: 1
          default: 100
          title: Page Size
      - name: search
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Search
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListWebhooksResponseDto'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/webhooks/{webhook_id}:
    get:
      tags:
      - Webhooks
      summary: Get webhook
      description: Returns full configuration of a single webhook by ID.
      operationId: getWebhook
      parameters:
      - name: webhook_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Webhook Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetWebhookResponseDto'
        '403':
          description: Webhook belongs to a different organization.
        '404':
          description: Webhook not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
    patch:
      tags:
      - Webhooks
      summary: Update webhook
      description: Partially updates a webhook's configuration. Only supplied fields
        are updated. Use is_active=false to disable delivery without deleting.
      operationId: updateWebhook
      parameters:
      - name: webhook_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Webhook Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateWebhookRequestDto'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateWebhookResponseDto'
        '403':
          description: Not authorized to update this webhook.
        '404':
          description: Webhook not found.
        '422':
          description: Validation error.
        '401':
          description: Missing or invalid authentication credentials.
    delete:
      tags:
      - Webhooks
      summary: Delete webhook
      description: Permanently deletes a webhook. All resource assignments are removed.
        Returns 204 No Content.
      operationId: deleteWebhook
      parameters:
      - name: webhook_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Webhook Id
      responses:
        '204':
          description: Successful Response
        '403':
          description: Not authorized to delete this webhook.
        '404':
          description: Webhook not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/webhooks/{webhook_id}/test:
    post:
      tags:
      - Webhooks
      summary: Test webhook delivery
      description: Sends a test HTTP request to the webhook URL and returns the response.
        Use this to verify URL reachability and auth before attaching to a live resource.
        Optionally supply a custom payload; otherwise a synthetic test payload is
        sent.
      operationId: testWebhook
      parameters:
      - name: webhook_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Webhook Id
      requestBody:
        content:
          application/json:
            schema:
              anyOf:
              - $ref: '#/components/schemas/TestWebhookRequestDto'
              - type: 'null'
              title: Request
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TestWebhookResponseDto'
        '403':
          description: Not authorized to test this webhook.
        '404':
          description: Webhook not found.
        '422':
          description: Test delivery failed — check the response body for upstream
            status code and error.
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/webhooks/{webhook_id}/resources:
    post:
      tags:
      - Webhooks
      summary: Assign resource to webhook
      description: Attaches a job, monitor, or monitor_group to a webhook so it receives
        delivery on completion. Returns 201 on new assignment, 200 if already assigned.
        A webhook can be assigned to multiple resources; each resource can have multiple
        webhooks (up to plan limit).
      operationId: assignWebhookResource
      parameters:
      - name: webhook_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Webhook Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssignWebhookResourceRequestDto'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssignWebhookResourceResponseDto'
        '400':
          description: Webhook per-resource limit exceeded.
        '403':
          description: Not authorized.
        '404':
          description: Webhook or resource not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
    get:
      tags:
      - Webhooks
      summary: List webhook resources
      description: Returns a paginated list of resources (jobs, monitors, monitor_groups)
        currently assigned to this webhook. Filter by resource_type.
      operationId: listWebhookResources
      parameters:
      - name: webhook_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Webhook Id
      - name: resource_type
        in: query
        required: false
        schema:
          anyOf:
          - $ref: '#/components/schemas/MappableResourceType'
          - type: 'null'
          title: Resource Type
      - name: page
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          default: 1
          title: Page
      - name: page_size
        in: query
        required: false
        schema:
          type: integer
          maximum: 500
          minimum: 1
          default: 100
          title: Page Size
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListWebhookResourcesResponseDto'
        '403':
          description: Not authorized.
        '404':
          description: Webhook not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/webhooks/{webhook_id}/resources/{resource_type}/{resource_id}:
    delete:
      tags:
      - Webhooks
      summary: Remove resource from webhook
      description: Detaches a specific resource from this webhook. Future job/monitor
        runs will no longer trigger delivery to this webhook. Returns 204 No Content.
      operationId: removeWebhookResource
      parameters:
      - name: webhook_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Webhook Id
      - name: resource_type
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/MappableResourceType'
      - name: resource_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Resource Id
      responses:
        '204':
          description: Successful Response
        '403':
          description: Not authorized.
        '404':
          description: Webhook not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/resources/{resource_type}/{resource_id}/webhooks:
    get:
      tags:
      - Webhooks
      summary: List webhooks for a resource
      description: Returns all webhooks currently assigned to the given resource (job,
        monitor, or monitor_group). Use is_active filter to see only active or inactive
        webhooks.
      operationId: listWebhooksForResource
      parameters:
      - name: resource_type
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/MappableResourceType'
      - name: resource_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Resource Id
      - name: is_active
        in: query
        required: false
        schema:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Is Active
      - name: page
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          default: 1
          title: Page
      - name: page_size
        in: query
        required: false
        schema:
          type: integer
          maximum: 500
          minimum: 1
          default: 100
          title: Page Size
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListWebhooksForResourceResponseDto'
        '403':
          description: Resource does not belong to your organization.
        '404':
          description: Resource not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/webhook-history:
    get:
      tags:
      - Webhooks
      summary: Get webhook delivery history
      description: Returns paginated webhook delivery events ordered by timestamp
        descending. Provide either `webhook_id` to fetch history for a specific webhook,
        or `resource_type` + `resource_id` to fetch history for a resource. Exactly
        one of these combinations must be supplied.
      operationId: getWebhookDeliveryHistory
      parameters:
      - name: webhook_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          description: Webhook ID — mutually exclusive with resource_type/resource_id
          title: Webhook Id
        description: Webhook ID — mutually exclusive with resource_type/resource_id
      - name: resource_type
        in: query
        required: false
        schema:
          anyOf:
          - $ref: '#/components/schemas/MappableResourceType'
          - type: 'null'
          description: 'Resource type: job, monitor, monitor_group'
          title: Resource Type
        description: 'Resource type: job, monitor, monitor_group'
      - name: resource_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          description: ID of the resource
          title: Resource Id
        description: ID of the resource
      - name: page
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          default: 1
          title: Page
      - name: page_size
        in: query
        required: false
        schema:
          type: integer
          maximum: 500
          minimum: 1
          default: 50
          title: Page Size
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                anyOf:
                - $ref: '#/components/schemas/WebhookHistoryByWebhookDto'
                - $ref: '#/components/schemas/WebhookHistoryByResourceDto'
                title: Response Getwebhookdeliveryhistory
        '403':
          description: Not authorized to view this resource's history.
        '404':
          description: Resource not found.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /catchAll/webhook/trigger/{resource_type}/{resource_id}:
    post:
      tags:
      - Webhooks
      summary: Manually trigger webhook delivery for a resource
      operationId: triggerWebhook
      parameters:
      - name: resource_type
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/MappableResourceType'
      - name: resource_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Resource Id
      - name: webhook_id
        in: query
        required: true
        schema:
          type: string
          format: uuid
          title: Webhook Id
      - name: job_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Job Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          description: Missing or invalid authentication credentials.
  /health:
    get:
      tags:
      - Meta
      summary: Check health
      description: Check API availability.
      operationId: healthCheck
      security: []
      responses:
        '200':
          description: API is healthy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  healthy:
                    type: boolean
                required:
                - healthy
  /version:
    get:
      tags:
      - Meta
      summary: Get version
      description: Returns current API version.
      operationId: getVersion
      security: []
      responses:
        '200':
          description: Current API version.
          content:
            application/json:
              schema:
                type: object
                properties:
                  version:
                    type: string
                required:
                - version
servers:
- url: https://catchall.newscatcherapi.com
  description: Production server
externalDocs:
  description: Find out more about NewsCatcher CatchAll API
  url: https://www.newscatcherapi.com/docs/web-search-api/get-started/introduction
tags:
- name: Jobs
  description: Operations to create, monitor, and retrieve job results.
  externalDocs:
    description: Learn about job lifecycle and status tracking
    url: https://www.newscatcherapi.com/docs/web-search-api/get-started/quickstart
- name: Monitors
  description: Operations to create, operate and retrieve monitor results.
  externalDocs:
    description: Automate recurring queries with scheduled jobs and webhook notifications.
    url: https://www.newscatcherapi.com/docs/web-search-api/guides-and-concepts/monitors
- name: Entities
  description: 'Operations to create, update, and delete company entities.


    Entities are the building blocks of Company Watchlist. Each entity represents
    a company (or person) you want to track. Add identifying information such as domain,
    alternative names, and key persons to improve matching quality.'
  externalDocs:
    description: Learn about Company Watchlist and entities
    url: https://www.newscatcherapi.com/docs/web-search-api/guides-and-concepts/company-search
- name: Datasets
  description: 'Operations to create and manage datasets of entities.


    A dataset is a named collection of entities — think of it as a watchlist or portfolio.
    Connect a dataset to a job via `connected_dataset_ids` to activate Company Watchlist.
    Datasets can be reused across multiple jobs and monitors.'
  externalDocs:
    description: Learn about datasets and Company Watchlist
    url: https://www.newscatcherapi.com/docs/web-search-api/guides-and-concepts/company-search
- name: Projects
  description: 'Operations to create, organize, and manage projects.


    A project is a named container for jobs, monitors, and datasets. Group related
    resources by use case, team, or client, and share them with teammates. Resources
    can be assigned at creation time or post-hoc.'
  externalDocs:
    description: Learn about projects and resource organization
    url: https://www.newscatcherapi.com/docs/web-search-api/guides-and-concepts/projects
- name: Webhooks
  description: 'Operations to create, manage, and test webhooks.


    Webhooks deliver real-time notifications to your endpoint when monitor jobs complete.
    Configure URL, authentication, headers, and delivery mode.'
  externalDocs:
    description: Learn about webhooks and notifications
    url: https://www.newscatcherapi.com/docs/web-search-api/guides-and-concepts/webhooks
- name: Meta
  description: Operations to check API health and version.
components:
  schemas:
    CheckQueryQualityResponseDto:
      properties:
        status:
          $ref: '#/components/schemas/QueryStatus'
        title:
          type: string
          title: Title
          description: Brief assessment headline
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: 2-3 sentence assessment
        issues:
          anyOf:
          - items:
              $ref: '#/components/schemas/IssueType'
            type: array
          - type: 'null'
          title: Issues
        suggestions:
          anyOf:
          - items:
              $ref: '#/components/schemas/Suggestion'
            type: array
          - type: 'null'
          title: Suggestions
        confidence:
          type: number
          maximum: 1.0
          minimum: 0.0
          title: Confidence
          description: Confidence in assessment
      type: object
      required:
      - status
      - title
      - confidence
      title: CheckQueryQualityResponseDto
      description: Complete query validation result
    CheckQueryQualityRequestDto:
      properties:
        query:
          type: string
          title: Query
      type: object
      required:
      - query
      title: CheckQueryQualityRequestDto
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          type: array
          title: Detail
      type: object
      title: HTTPValidationError
    DatasetEntityIdsRequestDto:
      properties:
        entity_ids:
          items:
            type: string
            format: uuid
          type: array
          title: Entity Ids
          description: List of entity IDs to add or remove.
      type: object
      required:
      - entity_ids
      title: DatasetEntityIdsRequest
    DisableMonitorResponseDto:
      properties:
        success:
          type: boolean
          title: Success
          description: True if the operation succeeded; false otherwise.
        message:
          type: string
          title: Message
          description: Human-readable success message.
        monitor_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Monitor Id
          description: ID of the disabled monitor.
      type: object
      required:
      - success
      - message
      title: DisableMonitorResponseDto
    AssignWebhookResourceRequestDto:
      properties:
        resource_type:
          $ref: '#/components/schemas/MappableResourceType'
          description: Type of resource to assign.
        resource_id:
          type: string
          format: uuid
          title: Resource Id
          description: ID of the resource to assign.
      type: object
      required:
      - resource_type
      - resource_id
      title: AssignWebhookResourceRequestDto
    Ownership:
      type: string
      enum:
      - all
      - own
      - shared
      title: Ownership
      description: Controls which resources are returned based on ownership. 'all'
        returns owned and shared resources, 'own' returns only user-created resources,
        'shared' returns only resources shared by others.
    ListUserJobsResponseDto:
      properties:
        total:
          type: integer
          title: Total
          description: Total number of jobs for this user.
        page:
          type: integer
          title: Page
          description: Current page number.
        page_size:
          type: integer
          title: Page Size
          description: Number of jobs per page.
        total_pages:
          type: integer
          title: Total Pages
          description: Total number of pages available.
        jobs:
          items:
            $ref: '#/components/schemas/UserJobResponseDto'
          type: array
          title: Jobs
          description: Array of user jobs on this page.
      type: object
      required:
      - total
      - page
      - page_size
      - total_pages
      - jobs
      title: ListUserJobsResponseDto
    UpdateProjectRequestDto:
      properties:
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
          description: New name for the project.
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: New description for the project.
      type: object
      title: UpdateProjectRequestDto
    PullMonitorResponseDto:
      properties:
        monitor_id:
          type: string
          format: uuid
          title: Monitor Id
          description: Unique identifier for the monitor.
        cron_expression:
          anyOf:
          - type: string
          - type: 'null'
          title: Cron Expression
          description: Cron expression for the monitor schedule.
        timezone:
          anyOf:
          - type: string
          - type: 'null'
          title: Timezone
          description: Timezone used for schedule execution.
        reference_job:
          $ref: '#/components/schemas/ReferenceJob'
          description: Query and context from the reference job.
        validators:
          items:
            $ref: '#/components/schemas/ValidatorSchema'
          type: array
          title: Validators
          description: Validators applied to filter results.
        enrichments:
          items:
            $ref: '#/components/schemas/EnrichmentSchema'
          type: array
          title: Enrichments
          description: Enrichment fields used for data extraction.
        run_info:
          anyOf:
          - $ref: '#/components/schemas/RunInfoDto'
          - type: 'null'
          description: Execution time range for this monitor.
        records:
          anyOf:
          - type: integer
          - type: 'null'
          title: Records
          description: Total number of records collected across all monitor jobs.
        status:
          type: string
          title: Status
          description: Current monitor status or error message.
        all_records:
          anyOf:
          - items:
              $ref: '#/components/schemas/MonitorRecordDto'
            type: array
          - type: 'null'
          title: All Records
          description: Aggregated records from all jobs executed by this monitor.
        limit:
          anyOf:
          - type: integer
          - type: 'null'
          title: Limit
          description: Record limit applied to this monitor's jobs.
        connected_datasets:
          anyOf:
          - items:
              $ref: '#/components/schemas/ConnectedDatasetDto'
            type: array
          - type: 'null'
          title: Connected Datasets
          description: Datasets used to narrow retrieval scope, each with id and name.
        is_all_news_query:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Is All News Query
          description: True when the reference job was submitted as an all-news (watchlist-generic)
            query.
        sharing_info:
          anyOf:
          - $ref: '#/components/schemas/SharingInfoDto'
          - type: 'null'
          description: Present when this monitor was shared with the authenticated
            user. Omitted when the user owns the monitor.
      type: object
      required:
      - monitor_id
      - reference_job
      - status
      title: PullMonitorResponseDto
    DatasetResponseDto:
      properties:
        id:
          type: string
          format: uuid
          title: Id
          description: Unique identifier of the dataset.
        organization_id:
          type: string
          title: Organization Id
          description: Organization that owns this dataset.
        name:
          type: string
          title: Name
          description: Dataset name.
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Optional description.
        entity_count:
          type: integer
          title: Entity Count
          description: Total number of entities in this dataset.
          default: 0
        entity_status_breakdown:
          additionalProperties:
            type: integer
          type: object
          title: Entity Status Breakdown
          description: Count of entities grouped by processing status.
        health_score:
          anyOf:
          - type: number
          - type: 'null'
          title: Health Score
          description: Overall health score of the dataset.
        health_breakdown:
          additionalProperties:
            type: number
          type: object
          title: Health Breakdown
          description: Health scores broken down by attribute.
        latest_status:
          anyOf:
          - type: string
          - type: 'null'
          title: Latest Status
          description: Most recent processing status of the dataset.
        created_by_user_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Created By User Id
          description: ID of the user who created this dataset.
        sharing_info:
          anyOf:
          - $ref: '#/components/schemas/SharingInfoDto'
          - type: 'null'
          description: Present when this dataset was shared with the authenticated
            user.
        created_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Created At
          description: Timestamp of when the dataset was created.
        updated_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Updated At
          description: Timestamp of when the dataset was last updated.
      type: object
      required:
      - id
      - organization_id
      - name
      title: DatasetResponse
    CreateMonitorRequestDto:
      properties:
        reference_job_id:
          type: string
          format: uuid
          title: Reference Job Id
          description: ID of a completed job whose query/validators/enrichments this
            monitor should repeat.
          example: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        schedule:
          type: string
          title: Schedule
          description: 'Natural-language schedule string converted to a cron expression.
            Examples: ''every day at 9 AM'', ''every Monday at 6 PM EST''. An inline
            timezone in the text overrides the timezone field.'
          example: every day at 9 AM
        timezone:
          anyOf:
          - type: string
          - type: 'null'
          title: Timezone
          description: IANA timezone for the schedule (e.g. 'America/New_York'). Overridden
            if the schedule text itself contains a timezone.
          default: UTC
          example: America/New_York
        webhook_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Webhook Ids
          description: Webhook IDs to notify on each run completion. Max 5 per monitor.
        limit:
          anyOf:
          - type: integer
            minimum: 10.0
          - type: 'null'
          title: Limit
          description: Max results per monitor run. Minimum 10. Validated against
            plan limit.
          example: 50
        backfill:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Backfill
          description: If true, the first run chains forward from the reference job's
            end date (enforces a 7-day lookback check). If false, the first run uses
            the current cron window.
          default: true
        project_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Project Id
          description: Project to associate this monitor with.
      type: object
      required:
      - reference_job_id
      - schedule
      title: CreateMonitorRequestDto
    EnableMonitorResponseDto:
      properties:
        success:
          type: boolean
          title: Success
          description: True if the operation succeeded; false otherwise.
        message:
          type: string
          title: Message
          description: Human-readable success message.
        monitor_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Monitor Id
          description: ID of the enabled monitor.
      type: object
      required:
      - success
      - message
      title: EnableMonitorResponseDto
    ListWebhooksResponseDto:
      properties:
        total:
          type: integer
          title: Total
          description: Total number of webhooks.
        page:
          type: integer
          title: Page
          description: Current page number.
        page_size:
          type: integer
          title: Page Size
          description: Number of webhooks per page.
        total_pages:
          type: integer
          title: Total Pages
          description: Total number of pages available.
        webhooks:
          items:
            $ref: '#/components/schemas/WebhookResponseDto'
          type: array
          title: Webhooks
          description: Array of webhook objects on this page.
      type: object
      required:
      - total
      - page
      - page_size
      - total_pages
      - webhooks
      title: ListWebhooksResponseDto
    GetProjectResponseDto:
      properties:
        success:
          type: boolean
          title: Success
          description: True if the operation succeeded; false otherwise.
        message:
          type: string
          title: Message
          description: Human-readable result message.
        project_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Project Id
          description: Project identifier.
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
          description: Project name.
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Project description.
        resources_count:
          type: integer
          title: Resources Count
          description: Number of resources assigned to this project.
          default: 0
        created_at:
          anyOf:
          - type: string
          - type: 'null'
          title: Created At
          description: Project creation timestamp.
        updated_at:
          anyOf:
          - type: string
          - type: 'null'
          title: Updated At
          description: Timestamp of the last update.
        sharing_info:
          anyOf:
          - $ref: '#/components/schemas/SharingInfoDto'
          - type: 'null'
          description: Present when this project was shared with the authenticated
            user.
      type: object
      required:
      - success
      - message
      title: GetProjectResponseDto
    DatasetListResponseDto:
      properties:
        datasets:
          items:
            $ref: '#/components/schemas/DatasetResponseDto'
          type: array
          title: Datasets
          description: Array of dataset objects for this page.
        total:
          type: integer
          title: Total
          description: Total number of datasets matching the filter criteria.
        page:
          type: integer
          title: Page
          description: Current page number.
        page_size:
          type: integer
          title: Page Size
          description: Number of datasets per page.
      type: object
      required:
      - datasets
      - total
      - page
      - page_size
      title: DatasetListResponse
    CreateDatasetRequestDto:
      properties:
        name:
          type: string
          minLength: 1
          title: Name
          description: Name for the dataset.
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Optional description.
        entity_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Entity Ids
          description: IDs of existing entities to include in the dataset.
        project_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Project Id
          description: Project to assign this dataset to.
      type: object
      required:
      - name
      title: CreateDatasetRequest
    CreateEntityRequestDto:
      properties:
        name:
          type: string
          minLength: 1
          title: Name
          description: Entity name. Required and must be non-empty.
        entity_type:
          type: string
          title: Entity Type
          description: The type of entity.
          default: company
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Free-text description for disambiguation. The entity must have
            this or additional_attributes.company_attributes.domain set.
        external_entity_id:
          anyOf:
          - type: string
            maxLength: 255
          - type: 'null'
          title: External Entity Id
          description: Optional external identifier for this entity. Free-form string,
            not enforced as unique.
        additional_attributes:
          anyOf:
          - $ref: '#/components/schemas/AdditionalAttributesDto'
          - type: 'null'
          description: Type-specific attributes for the entity.
      type: object
      required:
      - name
      title: CreateEntityRequest
    GetWebhookResponseDto:
      properties:
        success:
          type: boolean
          title: Success
          description: True if the operation succeeded; false otherwise.
        message:
          type: string
          title: Message
          description: Human-readable result message.
        webhook:
          anyOf:
          - $ref: '#/components/schemas/WebhookResponseDto'
          - type: 'null'
          description: Webhook details.
      type: object
      required:
      - success
      - message
      title: GetWebhookResponseDto
    CreateProjectResponseDto:
      properties:
        success:
          type: boolean
          title: Success
          description: True if the operation succeeded; false otherwise.
        message:
          type: string
          title: Message
          description: Human-readable result message.
        project_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Project Id
          description: Project identifier.
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
          description: Name of the created project.
      type: object
      required:
      - success
      - message
      title: CreateProjectResponseDto
    CreateProjectRequestDto:
      properties:
        name:
          type: string
          title: Name
          description: Name for the project.
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Optional description.
      type: object
      required:
      - name
      title: CreateProjectRequestDto
    PullResponseDto:
      properties:
        job_id:
          type: string
          format: uuid
          title: Job Id
          description: UUID of the job.
        query:
          anyOf:
          - type: string
          - type: 'null'
          title: Query
          description: Original query submitted for this job.
        context:
          anyOf:
          - type: string
          - type: 'null'
          title: Context
          description: Optional context provided at submission time.
        validators:
          items:
            $ref: '#/components/schemas/ValidatorSchema'
          type: array
          title: Validators
          description: Validators applied to candidate articles.
          default: []
        enrichments:
          items:
            $ref: '#/components/schemas/EnrichmentSchema'
          type: array
          title: Enrichments
          description: Enrichment fields extracted per article.
          default: []
        status:
          anyOf:
          - $ref: '#/components/schemas/PublicJobStatus'
          - type: 'null'
          description: Current pipeline stage. Results may be partial while status
            != 'completed'.
        error:
          anyOf:
          - type: string
          - type: 'null'
          title: Error
          description: Human-readable error message. Present only when status == 'failed'.
        duration:
          anyOf:
          - type: string
          - type: 'null'
          title: Duration
          description: Wall-clock processing time, e.g. '1m 23s'.
        candidate_records:
          anyOf:
          - type: integer
          - type: 'null'
          title: Candidate Records
          description: Total articles retrieved before validator filtering.
        valid_records:
          anyOf:
          - type: integer
          - type: 'null'
          title: Valid Records
          description: 'Articles that passed all validators. Invariant: valid_records
            <= candidate_records. Equals candidate_records when no validators are
            defined.'
        progress_validated:
          anyOf:
          - type: integer
          - type: 'null'
          title: Progress Validated
          description: Running count of validated records while status == 'enriching'.
        date_range:
          anyOf:
          - $ref: '#/components/schemas/DateRangeDto'
          - type: 'null'
          description: Effective date range of the retrieved articles.
        all_records:
          anyOf:
          - items:
              $ref: '#/components/schemas/ArticleRecordDto'
            type: array
          - type: 'null'
          title: All Records
          description: Paginated result records. Each record has a cluster identifier,
            title, job-specific enrichment values, and source citations.
        page:
          type: integer
          minimum: 1.0
          title: Page
          description: Current page number (1-indexed).
          default: 1
        page_size:
          type: integer
          minimum: 1.0
          title: Page Size
          description: Number of records per page.
          default: 10
        total_pages:
          type: integer
          minimum: 0.0
          title: Total Pages
          description: Total pages available.
          default: 0
        limit:
          anyOf:
          - type: integer
          - type: 'null'
          title: Limit
          description: Maximum records requested for this job.
        mode:
          $ref: '#/components/schemas/ProcessingMode'
          description: 'Processing mode used: ''base'' or ''lite''.'
          default: base
        connected_dataset_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Connected Dataset Ids
          description: 'Deprecated: use connected_datasets instead. Dataset IDs that
            were used to narrow retrieval scope.'
          deprecated: true
        connected_datasets:
          anyOf:
          - items:
              $ref: '#/components/schemas/ConnectedDatasetDto'
            type: array
          - type: 'null'
          title: Connected Datasets
          description: Datasets used to narrow retrieval scope, each with id and name.
        is_all_news_query:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Is All News Query
          description: True when the query was submitted as an all-news (watchlist-generic)
            query.
        sharing_info:
          anyOf:
          - $ref: '#/components/schemas/SharingInfoDto'
          - type: 'null'
          description: Present when the job was shared with the calling user (not
            their own).
      type: object
      required:
      - job_id
      title: PullJobResponseDto
    EntityListResponseDto:
      properties:
        entities:
          items:
            $ref: '#/components/schemas/EntityResponseDto'
          type: array
          title: Entities
          description: Array of entity objects for this page.
        total:
          type: integer
          title: Total
          description: Total number of entities matching the filter criteria.
        page:
          type: integer
          title: Page
          description: Current page number.
        page_size:
          type: integer
          title: Page Size
          description: Number of entities per page.
      type: object
      required:
      - entities
      - total
      - page
      - page_size
      title: EntityListResponse
    ManageEntitiesResponseDto:
      properties:
        dataset_id:
          type: string
          format: uuid
          title: Dataset Id
          description: ID of the dataset that was modified.
        affected_count:
          type: integer
          title: Affected Count
          description: Number of entities that were added or removed.
      type: object
      required:
      - dataset_id
      - affected_count
      title: ManageEntitiesResponse
    ContinueRequestDto:
      properties:
        job_id:
          type: string
          format: uuid
          title: Job Id
          description: Existing job to extend with a higher result limit.
        new_limit:
          anyOf:
          - type: integer
          - type: 'null'
          title: New Limit
          description: New limit, must be greater than the current limit. If null,
            raises to the plan maximum.
          example: 250
      type: object
      required:
      - job_id
      title: ContinueRequestDto
      example:
        job_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        new_limit: 250
    DeleteProjectResponseDto:
      properties:
        success:
          type: boolean
          title: Success
          description: True if the operation succeeded; false otherwise.
        message:
          type: string
          title: Message
          description: Human-readable result message.
        project_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Project Id
          description: ID of the deleted project.
        deleted_resources:
          anyOf:
          - additionalProperties:
              type: integer
            type: object
          - type: 'null'
          title: Deleted Resources
          description: Count of deleted resources grouped by type (job, monitor, dataset,
            monitor_group). Includes a 'webhook_unlinked' count for webhooks that
            were detached from the project (not deleted) as part of this operation
            — webhooks are never deleted when a project is deleted, since a webhook
            may be attached to other resources independently of this project.
      type: object
      required:
      - success
      - message
      title: DeleteProjectResponseDto
    WebhookHistoryByResourceDto:
      properties:
        mode:
          type: string
          const: resource
          title: Mode
          default: resource
        resource_type:
          $ref: '#/components/schemas/MappableResourceType'
        resource_id:
          type: string
          format: uuid
          title: Resource Id
        total:
          type: integer
          title: Total
        page:
          type: integer
          title: Page
        page_size:
          type: integer
          title: Page Size
        items:
          items:
            $ref: '#/components/schemas/WebhookStatusItemDto'
          type: array
          title: Items
      type: object
      required:
      - resource_type
      - resource_id
      - total
      - page
      - page_size
      - items
      title: WebhookHistoryByResourceDto
    ProjectListResponseDto:
      properties:
        total:
          type: integer
          title: Total
          description: Total number of projects matching the filter criteria.
        page:
          type: integer
          title: Page
          description: Current page number.
        page_size:
          type: integer
          title: Page Size
          description: Number of projects per page.
        total_pages:
          type: integer
          title: Total Pages
          description: Total number of pages available.
        projects:
          items:
            $ref: '#/components/schemas/ProjectResponseDto'
          type: array
          title: Projects
          description: Projects on this page.
      type: object
      required:
      - total
      - page
      - page_size
      - total_pages
      - projects
      title: ProjectListResponseDto
    UpdateMonitorResponseDto:
      properties:
        monitor_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Monitor Id
          description: Monitor identifier.
        status:
          type: string
          title: Status
          description: Confirmation message.
          default: Monitor updated Successfully
      type: object
      title: UpdateMonitorResponseDto
    CreateEntitiesBatchRequestDto:
      properties:
        entities:
          items:
            $ref: '#/components/schemas/CreateEntityRequestDto'
          type: array
          title: Entities
          description: Array of entities to create.
      type: object
      required:
      - entities
      title: CreateEntitiesBatchRequest
    CreateWebhookResponseDto:
      properties:
        success:
          type: boolean
          title: Success
          description: True if the operation succeeded; false otherwise.
        message:
          type: string
          title: Message
          description: Human-readable result message.
        webhook:
          anyOf:
          - $ref: '#/components/schemas/WebhookResponseDto'
          - type: 'null'
          description: Created webhook details.
      type: object
      required:
      - success
      - message
      title: CreateWebhookResponseDto
    UpdateEntityRequestDto:
      properties:
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
          description: Updated entity name.
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Updated description.
        external_entity_id:
          anyOf:
          - type: string
            maxLength: 255
          - type: 'null'
          title: External Entity Id
          description: Updated external identifier for this entity.
        additional_attributes:
          anyOf:
          - $ref: '#/components/schemas/AdditionalAttributesDto'
          - type: 'null'
          description: Updated type-specific attributes.
      type: object
      title: UpdateEntityRequest
    ListWebhookResourcesResponseDto:
      properties:
        total:
          type: integer
          title: Total
          description: Total number of resource mappings.
        page:
          type: integer
          title: Page
          description: Current page number.
        page_size:
          type: integer
          title: Page Size
          description: Number of mappings per page.
        total_pages:
          type: integer
          title: Total Pages
          description: Total number of pages available.
        resources:
          items:
            $ref: '#/components/schemas/WebhookResourceMappingResponseDto'
          type: array
          title: Resources
          description: Array of resource mappings on this page.
      type: object
      required:
      - total
      - page
      - page_size
      - total_pages
      - resources
      title: ListWebhookResourcesResponseDto
    EnableMonitorRequestDto:
      properties:
        backfill:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Backfill
          description: If True, enforce lookback check on last job; if False, skip
            date check.
          default: true
      type: object
      title: EnableMonitorRequestDto
      description: Optional body for POST /{monitor_id}/enable.
    CreateEntitiesBatchResponseDto:
      properties:
        entities:
          items:
            $ref: '#/components/schemas/CreateEntityResponseDto'
          type: array
          title: Entities
          description: Array of created entity stubs in the same order as the input.
        count:
          type: integer
          title: Count
          description: Total number of entities created.
      type: object
      required:
      - entities
      - count
      title: CreateEntitiesBatchResponse
    UpdateDatasetRequestDto:
      properties:
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
          description: Updated dataset name.
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Updated description.
      type: object
      title: UpdateDatasetRequest
    UpdateWebhookRequestDto:
      properties:
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
          description: Updated webhook name.
        url:
          anyOf:
          - type: string
          - type: 'null'
          title: Url
          description: Updated webhook endpoint URL.
        type:
          anyOf:
          - $ref: '#/components/schemas/WebhookType'
          - type: 'null'
          description: Updated webhook target type.
        delivery_mode:
          anyOf:
          - $ref: '#/components/schemas/DeliveryMode'
          - type: 'null'
          description: Updated delivery mode.
        method:
          anyOf:
          - $ref: '#/components/schemas/HttpMethod'
          - type: 'null'
          description: Updated HTTP method.
        headers:
          anyOf:
          - additionalProperties:
              type: string
            type: object
          - type: 'null'
          title: Headers
          description: Updated HTTP headers.
        params:
          anyOf:
          - additionalProperties:
              type: string
            type: object
          - type: 'null'
          title: Params
          description: Updated query string parameters.
        auth:
          anyOf:
          - oneOf:
            - $ref: '#/components/schemas/BearerAuthDto'
            - $ref: '#/components/schemas/ApiKeyAuthDto'
            - $ref: '#/components/schemas/BasicAuthDto'
            discriminator:
              propertyName: type
              mapping:
                api_key: '#/components/schemas/ApiKeyAuthDto'
                basic: '#/components/schemas/BasicAuthDto'
                bearer: '#/components/schemas/BearerAuthDto'
          - type: 'null'
          title: Auth
          description: 'Authentication forwarded with each delivery. Supported types:
            ''bearer'' (token), ''api_key'' (header + value), ''basic'' (username
            + password).'
        formatter_config:
          anyOf:
          - $ref: '#/components/schemas/FormatterConfigDto'
          - type: 'null'
          description: Updated custom formatter configuration.
        is_active:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Is Active
          description: Set to true to activate or false to deactivate.
      type: object
      title: UpdateWebhookRequestDto
    ProjectResourceTypeEnum:
      type: string
      enum:
      - job
      - monitor
      - dataset
      - monitor_group
      - webhook
      title: ProjectResourceTypeEnum
      description: Resource type for project association.
    UpdateWebhookResponseDto:
      properties:
        success:
          type: boolean
          title: Success
          description: True if the operation succeeded; false otherwise.
        message:
          type: string
          title: Message
          description: Human-readable result message.
        webhook:
          anyOf:
          - $ref: '#/components/schemas/WebhookResponseDto'
          - type: 'null'
          description: Updated webhook details.
      type: object
      required:
      - success
      - message
      title: UpdateWebhookResponseDto
    DeleteJobResponseDto:
      properties:
        success:
          type: boolean
          title: Success
          description: True if the delete operation succeeded; false otherwise.
        message:
          type: string
          title: Message
          description: Human-readable result message.
        job_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Job Id
          description: ID of the deleted job. Null on failure.
      type: object
      required:
      - success
      - message
      title: DeleteJobResponseDto
    AssignWebhookResourceResponseDto:
      properties:
        success:
          type: boolean
          title: Success
          description: True if the operation succeeded; false otherwise.
        message:
          anyOf:
          - type: string
          - type: 'null'
          title: Message
          description: Human-readable result message.
        already_existed:
          type: boolean
          title: Already Existed
          description: True if the mapping already existed.
          default: false
        mapping:
          anyOf:
          - $ref: '#/components/schemas/WebhookResourceMappingResponseDto'
          - type: 'null'
          description: Created or existing resource mapping.
      type: object
      required:
      - success
      title: AssignWebhookResourceResponseDto
    DatasetEntityListResponseDto:
      properties:
        entities:
          items:
            $ref: '#/components/schemas/EntitySummaryDto'
          type: array
          title: Entities
          description: Array of entity summary objects for this page.
        total:
          type: integer
          title: Total
          description: Total number of entities in the dataset.
        page:
          type: integer
          title: Page
          description: Current page number.
        page_size:
          type: integer
          title: Page Size
          description: Number of entities per page.
      type: object
      required:
      - entities
      - total
      - page
      - page_size
      title: DatasetEntityListResponse
    TestWebhookResponseDto:
      properties:
        success:
          type: boolean
          title: Success
          description: True if the test delivery succeeded; false otherwise.
        message:
          type: string
          title: Message
          description: Human-readable result message.
        http_status_code:
          anyOf:
          - type: integer
          - type: 'null'
          title: Http Status Code
          description: HTTP status code returned by the webhook endpoint.
        response_body:
          anyOf:
          - {}
          - type: 'null'
          title: Response Body
          description: Response body returned by the webhook endpoint.
      type: object
      required:
      - success
      - message
      title: TestWebhookResponseDto
    EntitySortBy:
      type: string
      enum:
      - created_at
      - name
      - status
      title: EntitySortBy
      description: Fields available for sorting entity list results.
    ListMonitorJobsResponseDto:
      properties:
        monitor_id:
          type: string
          format: uuid
          title: Monitor Id
          description: Monitor identifier.
        sort_order:
          type: string
          title: Sort Order
          description: Sort order applied to the job list.
        total_jobs:
          type: integer
          title: Total Jobs
          description: Total number of jobs executed by this monitor.
        jobs:
          items:
            $ref: '#/components/schemas/MonitorJobItemResponseDto'
          type: array
          title: Jobs
          description: Array of monitor job executions.
      type: object
      required:
      - monitor_id
      - sort_order
      - total_jobs
      title: ListMonitorJobsResponseDto
    ContinueResponseDto:
      properties:
        job_id:
          type: string
          format: uuid
          title: Job Id
          description: UUID of the continued job.
        previous_limit:
          anyOf:
          - type: integer
          - type: 'null'
          title: Previous Limit
          description: The limit before this continue request.
        new_limit:
          type: integer
          title: New Limit
          description: The new limit now in effect.
        status:
          type: string
          title: Status
          description: Always 'accepted' on success.
          default: accepted
      type: object
      required:
      - job_id
      - new_limit
      title: ContinueResponseDto
    ProcessingMode:
      type: string
      enum:
      - base
      - lite
      title: ProcessingMode
      description: Job processing mode. 'base' runs the full pipeline with clustering
        and enrichment. 'lite' is a faster mode that skips clustering.
    ListWebhooksForResourceResponseDto:
      properties:
        total:
          type: integer
          title: Total
          description: Total number of webhooks assigned to this resource.
        page:
          type: integer
          title: Page
          description: Current page number.
        page_size:
          type: integer
          title: Page Size
          description: Number of webhooks per page.
        total_pages:
          type: integer
          title: Total Pages
          description: Total number of pages available.
        webhooks:
          items:
            $ref: '#/components/schemas/WebhookResponseDto'
          type: array
          title: Webhooks
          description: Array of webhook objects.
      type: object
      required:
      - total
      - page
      - page_size
      - total_pages
      - webhooks
      title: ListWebhooksForResourceResponseDto
    AddResourceRequestDto:
      properties:
        resources:
          items:
            $ref: '#/components/schemas/ResourceItemRequestDto'
          type: array
          title: Resources
          description: Resources to assign to the project.
      type: object
      required:
      - resources
      title: AddResourceRequestDto
    SubmitRequestDto:
      properties:
        query:
          type: string
          minLength: 1
          title: Query
          description: Natural-language question. Keywords and search queries are
            extracted automatically.
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Schema
          description: 'Advanced: custom JSON schema string that overrides the default
            extraction schema. Use POST /initialize to discover a suitable schema.'
        context:
          anyOf:
          - type: string
          - type: 'null'
          title: Context
          description: Additional context to sharpen the search scope (geography,
            industry, time frame, etc.).
        limit:
          anyOf:
          - type: integer
            minimum: 10.0
          - type: 'null'
          title: Limit
          description: Maximum number of validated records to collect. Minimum 10.
            Defaults to the plan limit. Use POST /continue to raise the limit later.
        start_date:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Start Date
          description: Earliest article date (ISO-8601 or free-text like '2024-01-01').
            If start_date is provided and end_date is omitted, end_date defaults to
            now.
        end_date:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: End Date
          description: Latest article date. Cannot be provided without start_date.
        validators:
          anyOf:
          - items:
              $ref: '#/components/schemas/ValidatorSchema'
            type: array
          - type: 'null'
          title: Validators
          description: Boolean classifiers. Only articles where every validator returns
            true are counted in valid_records. Provide at least one item or omit entirely.
        enrichments:
          anyOf:
          - items:
              $ref: '#/components/schemas/EnrichmentSchema'
            type: array
          - type: 'null'
          title: Enrichments
          description: Custom extraction fields added to each result record under
            enrichments.<name>. Provide at least one item or omit entirely.
        mode:
          anyOf:
          - $ref: '#/components/schemas/ProcessingMode'
          - type: 'null'
          description: Processing mode. 'base' runs the full pipeline with clustering
            and enrichment. 'lite' is faster with no clustering.
          default: base
        connected_dataset_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Connected Dataset Ids
          description: IDs of datasets whose entities narrow the retrieval scope.
            When provided, ed_score_min defaults to 2 if not specified.
        ed_score_min:
          anyOf:
          - type: integer
            maximum: 10.0
            minimum: 1.0
          - type: 'null'
          title: Ed Score Min
          description: Minimum entity-domain relevance score (1–10). Only relevant
            when connected_dataset_ids is set. Defaults to 2.
        ed_association_type:
          anyOf:
          - $ref: '#/components/schemas/EntityAssociationType'
          - type: 'null'
          description: Filter events by entity association type. 'event_associated'
            keeps only events where the entity is a direct actor. 'mention' keeps
            only events where the entity is merely referenced. Only relevant when
            connected_dataset_ids is set.
        webhook_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Webhook Ids
          description: Webhook IDs to notify when the job completes. Max 5 per job.
        project_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Project Id
          description: Project ID to associate this job with.
        fetch_all_watchlist_news:
          type: boolean
          title: Fetch All Watchlist News
          description: When true, retrieves all news for connected Company Watchlist
            entities without topic filtering. Requires connected_dataset_ids to be
            set.
          default: false
      type: object
      required:
      - query
      title: SubmitRequestDto
      example:
        context: Focus on US-based companies with 500+ employees.
        enrichments:
        - description: The company conducting the layoffs.
          name: company_name
          type: company
        - description: Number of employees affected, if mentioned.
          name: employees_affected
          type: number
        limit: 100
        mode: base
        query: Tech layoffs in 2024
        start_date: '2024-01-01T00:00:00Z'
        validators:
        - description: The article specifically covers workforce reductions or layoffs.
          name: is_about_layoffs
          type: boolean
    SubmitResponseDto:
      properties:
        job_id:
          type: string
          format: uuid
          title: Job Id
          description: UUID of the created job. Use this in /status and /pull.
          example: 3fa85f64-5717-4562-b3fc-2c963f66afa6
      type: object
      required:
      - job_id
      title: SubmitResponseDto
    DatasetSortBy:
      type: string
      enum:
      - name
      - created_at
      - status
      title: DatasetSortBy
      description: Fields available for sorting dataset list results.
    SuggestInitializeRequestDto:
      properties:
        query:
          type: string
          title: Query
          description: Natural-language question to analyse. The API returns the validators
            and enrichments it would auto-generate for this query.
          example: Which airlines expanded their routes in Asia?
        context:
          anyOf:
          - type: string
          - type: 'null'
          title: Context
          description: Optional context string that sharpens the suggested fields.
          example: Focus on low-cost carriers.
        connected_dataset_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Connected Dataset Ids
          description: Optional list of watchlist dataset IDs connected to this job.
        fetch_all_watchlist_news:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Fetch All Watchlist News
          description: When true, returns generic news validators and enrichments
            suitable for watchlist-based article collection instead of query-specific
            fields.
      type: object
      required:
      - query
      title: InitializeRequestDto
      example:
        context: Focus on low-cost carriers.
        query: Which airlines expanded their routes in Asia?
    ListDatasetEntitiesRequestDto:
      properties:
        page:
          type: integer
          minimum: 1.0
          title: Page
          description: The page number to retrieve.
          default: 1
        page_size:
          type: integer
          maximum: 500.0
          minimum: 1.0
          title: Page Size
          description: The number of entities per page.
          default: 100
        search:
          anyOf:
          - type: string
          - type: 'null'
          title: Search
          description: Filters entities by name using case-insensitive substring match.
        status:
          anyOf:
          - $ref: '#/components/schemas/EntityStatus'
          - type: 'null'
          description: Filter entities by processing status.
        entity_type:
          anyOf:
          - $ref: '#/components/schemas/EntityType'
          - type: 'null'
          description: Filter entities by type.
        sort_by:
          $ref: '#/components/schemas/EntitySortBy'
          description: Field to sort results by.
          default: created_at
        sort_order:
          $ref: '#/components/schemas/SortOrder'
          description: Sort direction.
          default: desc
      type: object
      title: ListDatasetEntitiesRequest
    AddResourceResponseDto:
      properties:
        success:
          type: boolean
          title: Success
          description: True if the overall operation succeeded.
        message:
          type: string
          title: Message
          description: Top-level result message.
        results:
          items:
            $ref: '#/components/schemas/ResourceResultResponseDto'
          type: array
          title: Results
          description: Per-resource outcome, one entry per submitted resource.
      type: object
      required:
      - success
      - message
      title: AddResourceResponseDto
    RemoveResourceResponseDto:
      properties:
        success:
          type: boolean
          title: Success
          description: True if the operation succeeded; false otherwise.
        message:
          type: string
          title: Message
          description: Human-readable result message.
      type: object
      required:
      - success
      - message
      title: RemoveResourceResponseDto
    SuggestInitializeResponseDto:
      properties:
        query:
          type: string
          title: Query
          description: Echo of the query from the request.
        context:
          anyOf:
          - type: string
          - type: 'null'
          title: Context
          description: Echo of the context from the request. Null if not provided.
        start_date:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Start Date
          description: Suggested start date for web search in ISO 8601 format.
        end_date:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: End Date
          description: Suggested end date for web search in ISO 8601 format.
        validators:
          items:
            $ref: '#/components/schemas/ValidatorSchema'
          type: array
          title: Validators
          description: Suggested validators for filtering relevant web pages.
        enrichments:
          items:
            $ref: '#/components/schemas/EnrichmentSchema'
          type: array
          title: Enrichments
          description: Suggested enrichment fields for data extraction.
        date_modification_message:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Date Modification Message
          description: Messages explaining date adjustments due to plan limits.
      type: object
      required:
      - query
      title: InitializeResponseDto
    CreateMonitorResponseDto:
      properties:
        monitor_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Monitor Id
          description: Monitor identifier. Null if creation failed.
        status:
          type: string
          title: Status
          description: Creation status or error message.
          default: Monitor Created Successfully
      type: object
      title: CreateMonitorResponseDto
    MappableResourceType:
      type: string
      enum:
      - job
      - monitor
      - monitor_group
      title: MappableResourceType
      description: 'Resource types that can be assigned to a webhook: job, monitor,
        or monitor_group.'
    TestWebhookRequestDto:
      properties:
        payload:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Payload
          description: Custom payload to send in the test request.
      type: object
      title: TestWebhookRequestDto
    CreateEntityResponseDto:
      properties:
        id:
          type: string
          format: uuid
          title: Id
          description: Unique identifier of the created entity.
        status:
          type: string
          title: Status
          description: Initial status of the entity. Always pending immediately after
            creation.
          default: pending
      type: object
      required:
      - id
      title: CreateEntityResponse
    ResourceListResponseDto:
      properties:
        total:
          type: integer
          title: Total
          description: Total number of resources assigned to the project.
        page:
          type: integer
          title: Page
          description: Current page number.
        page_size:
          type: integer
          title: Page Size
          description: Number of resources per page.
        total_pages:
          type: integer
          title: Total Pages
          description: Total number of pages available.
        resources:
          items:
            $ref: '#/components/schemas/ResourceItemResponseDto'
          type: array
          title: Resources
          description: Resources on this page.
      type: object
      required:
      - total
      - page
      - page_size
      - total_pages
      - resources
      title: ProjectResourceListResponseDto
    DatasetStatus:
      type: string
      enum:
      - pending
      - enriching
      - ready
      - failed
      title: DatasetStatus
      description: 'Processing status of a dataset: pending → enriching → ready. Failed
        indicates one or more entity enrichments failed.'
    ListMonitorsResponseDto:
      properties:
        total:
          type: integer
          title: Total
          description: Total number of monitors for this user.
        page:
          type: integer
          title: Page
          description: Current page number.
        page_size:
          type: integer
          title: Page Size
          description: Number of monitors per page.
        total_pages:
          type: integer
          title: Total Pages
          description: Total number of pages available.
        monitors:
          items:
            $ref: '#/components/schemas/MonitorListItemResponseDto'
          type: array
          title: Monitors
          description: Array of monitor summaries.
      type: object
      required:
      - total
      - page
      - page_size
      - total_pages
      - monitors
      title: ListMonitorsResponseDto
    CsvUploadCreateResponseDto:
      properties:
        dataset_id:
          type: string
          format: uuid
          title: Dataset Id
          description: Unique identifier of the created dataset.
        dataset_name:
          type: string
          title: Dataset Name
          description: Name of the created dataset.
        entities_created:
          type: integer
          title: Entities Created
          description: Number of entities successfully created from the CSV.
        validation_report:
          $ref: '#/components/schemas/CsvValidationReportDto'
          description: Summary of CSV processing results.
      type: object
      required:
      - dataset_id
      - dataset_name
      - entities_created
      - validation_report
      title: CreateDatasetCsvResponse
    EntityType:
      type: string
      enum:
      - company
      - person
      title: EntityType
      description: The type of entity. 'company' for organizations (default), 'person'
        for individuals.
    ProjectOverviewResponseDto:
      properties:
        project_id:
          type: string
          format: uuid
          title: Project Id
          description: Project identifier.
        overview:
          additionalProperties:
            additionalProperties:
              type: integer
            type: object
          type: object
          title: Overview
          description: Resource counts grouped by type.
      type: object
      required:
      - project_id
      - overview
      title: ProjectOverviewResponseDto
    Body_createDatasetFromCsv:
      properties:
        file:
          type: string
          format: binary
          title: File
        name:
          type: string
          title: Name
        description:
          type: string
          title: Description
        project_id:
          type: string
          title: Project Id
      type: object
      required:
      - file
      - name
      title: Body_createDatasetFromCsv
    UpdateProjectResponseDto:
      properties:
        success:
          type: boolean
          title: Success
          description: True if the operation succeeded; false otherwise.
        message:
          type: string
          title: Message
          description: Human-readable result message.
        project_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Project Id
          description: Project identifier.
      type: object
      required:
      - success
      - message
      title: UpdateProjectResponseDto
    SortOrder:
      type: string
      enum:
      - asc
      - desc
      title: SortOrder
      description: Sort direction for list results. 'asc' for ascending (oldest first),
        'desc' for descending (newest first).
    EntityStatus:
      type: string
      enum:
      - pending
      - enriching
      - ready
      - failed
      title: EntityStatus
      description: 'Processing status of an entity: pending → enriching → ready. Failed
        entities may still be used but matching quality may be reduced.'
    MonitorStatusHistoryResponseDto:
      properties:
        success:
          type: boolean
          title: Success
          description: True if the request succeeded; false otherwise.
        message:
          anyOf:
          - type: string
          - type: 'null'
          title: Message
          description: Optional message. Null on success.
        monitor_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Monitor Id
          description: Monitor identifier. Null on failure.
        total_statuses:
          anyOf:
          - type: integer
          - type: 'null'
          title: Total Statuses
          description: Total number of status entries in the history.
        statuses:
          anyOf:
          - items:
              $ref: '#/components/schemas/MonitorStatusEntryDto'
            type: array
          - type: 'null'
          title: Statuses
          description: Full status history, ordered newest to oldest.
      type: object
      required:
      - success
      title: MonitorStatusHistoryResponseDto
    Body_uploadCSVToDataset:
      properties:
        file:
          type: string
          format: binary
          title: File
      type: object
      required:
      - file
      title: Body_uploadCSVToDataset
    EntityResponseDto:
      properties:
        id:
          type: string
          format: uuid
          title: Id
          description: Unique identifier of the entity.
        entity_type:
          type: string
          title: Entity Type
          description: The type of entity.
        organization_id:
          type: string
          title: Organization Id
          description: Organization that owns this entity.
        name:
          type: string
          title: Name
          description: Entity name.
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Free-text description.
        external_entity_id:
          anyOf:
          - type: string
          - type: 'null'
          title: External Entity Id
          description: External identifier for this entity. Null when not set.
        additional_attributes:
          anyOf:
          - $ref: '#/components/schemas/AdditionalAttributesDto'
          - type: 'null'
          description: Type-specific attributes.
        status:
          $ref: '#/components/schemas/EntityStatus'
          description: Processing status of the entity.
        created_by_user_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Created By User Id
          description: ID of the user who created this entity.
        created_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Created At
          description: Timestamp of when the entity was created.
        updated_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Updated At
          description: Timestamp of when the entity was last updated.
      type: object
      required:
      - id
      - entity_type
      - organization_id
      - name
      - status
      title: EntityResponse
    DeleteMonitorResponseDto:
      properties:
        success:
          type: boolean
          title: Success
          description: True if the delete operation succeeded; false otherwise.
        message:
          type: string
          title: Message
          description: Human-readable result message.
        monitor_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Monitor Id
          description: ID of the deleted monitor. Null on failure.
      type: object
      required:
      - success
      - message
      title: DeleteMonitorResponseDto
    UpdateMonitorRequestDto:
      properties:
        webhook_ids:
          anyOf:
          - items:
              type: string
              format: uuid
            type: array
          - type: 'null'
          title: Webhook Ids
          description: Optional list of centralized webhook IDs to assign to this
            monitor. Empty list clears assignments.
        limit:
          anyOf:
          - type: integer
          - type: 'null'
          title: Limit
          description: Optional max results per monitor run (min 10, validated against
            plan)
      type: object
      title: UpdateMonitorRequestDto
    CreateWebhookRequestDto:
      properties:
        name:
          type: string
          title: Name
          description: Human-readable label for this webhook.
          example: Layoffs Alert
        url:
          type: string
          title: Url
          description: Destination URL that receives the payload.
          example: https://hooks.slack.com/services/T000/B000/xxxx
        type:
          $ref: '#/components/schemas/WebhookType'
          default: generic
        delivery_mode:
          $ref: '#/components/schemas/DeliveryMode'
          description: '''full'' delivers the entire result set in one call; ''per_record''
            sends one call per article.'
          default: full
        method:
          $ref: '#/components/schemas/HttpMethod'
          description: HTTP method used for delivery.
          default: POST
        headers:
          additionalProperties:
            type: string
          type: object
          title: Headers
          description: Custom HTTP headers forwarded with each delivery.
          default: {}
        params:
          additionalProperties:
            type: string
          type: object
          title: Params
          description: Query parameters appended to the webhook URL.
          default: {}
        auth:
          anyOf:
          - oneOf:
            - $ref: '#/components/schemas/BearerAuthDto'
            - $ref: '#/components/schemas/ApiKeyAuthDto'
            - $ref: '#/components/schemas/BasicAuthDto'
            discriminator:
              propertyName: type
              mapping:
                api_key: '#/components/schemas/ApiKeyAuthDto'
                basic: '#/components/schemas/BasicAuthDto'
                bearer: '#/components/schemas/BearerAuthDto'
          - type: 'null'
          title: Auth
          description: 'Authentication forwarded with each delivery. Supported types:
            ''bearer'' (token), ''api_key'' (header + value), ''basic'' (username
            + password).'
        formatter_config:
          anyOf:
          - $ref: '#/components/schemas/FormatterConfigDto'
          - type: 'null'
          description: Custom payload formatter. Required when type is 'custom'.
        project_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Project Id
          description: Optional project ID to associate this webhook with immediately
            upon creation.
      type: object
      required:
      - name
      - url
      title: CreateWebhookRequestDto
      example:
        delivery_mode: full
        name: Layoffs Alert
        type: slack
        url: https://hooks.slack.com/services/T000/B000/xxxx
    CsvUploadToExistingResponseDto:
      properties:
        dataset_id:
          type: string
          format: uuid
          title: Dataset Id
          description: ID of the dataset that was updated.
        entities_created:
          type: integer
          title: Entities Created
          description: Number of new entities created from the CSV.
        validation_report:
          $ref: '#/components/schemas/CsvValidationReportDto'
          description: Summary of CSV processing results.
      type: object
      required:
      - dataset_id
      - entities_created
      - validation_report
      title: UploadCsvToDatasetResponse
    DatasetStatusHistoryResponseDto:
      properties:
        dataset_id:
          type: string
          format: uuid
          title: Dataset Id
          description: Dataset identifier.
        history:
          items:
            $ref: '#/components/schemas/DatasetStatusEntryDto'
          type: array
          title: History
          description: Status change entries, ordered oldest to newest.
      type: object
      required:
      - dataset_id
      - history
      title: DatasetStatusHistoryResponse
    WebhookHistoryByWebhookDto:
      properties:
        mode:
          type: string
          const: webhook
          title: Mode
          default: webhook
        webhook_id:
          type: string
          format: uuid
          title: Webhook Id
        total:
          type: integer
          title: Total
        page:
          type: integer
          title: Page
        page_size:
          type: integer
          title: Page Size
        items:
          items:
            $ref: '#/components/schemas/WebhookStatusItemDto'
          type: array
          title: Items
      type: object
      required:
      - webhook_id
      - total
      - page
      - page_size
      - items
      title: WebhookHistoryByWebhookDto
    StatusResponseDto:
      properties:
        job_id:
          type: string
          format: uuid
          title: Job Id
          description: UUID of the job.
          example: 3fa85f64-5717-4562-b3fc-2c963f66afa6
        status:
          anyOf:
          - $ref: '#/components/schemas/PublicJobStatus'
          - type: 'null'
          description: 'Current pipeline stage. Poll until ''completed'' or ''failed''.
            Recommended interval: 5 seconds.'
        steps:
          anyOf:
          - items:
              $ref: '#/components/schemas/JobStep'
            type: array
          - type: 'null'
          title: Steps
          description: Ordered pipeline stages with completion flags. Use to show
            a progress bar to the user.
      type: object
      required:
      - job_id
      title: StatusResponseDto
    GetFeaturesResponse:
      properties:
        features:
          items:
            $ref: '#/components/schemas/BillingFeatureWithUsage'
          type: array
          title: Features
          description: Plan features with current usage.
      type: object
      required:
      - features
      title: GetPlanLimitsResponseDto
    IssueType:
      type: string
      enum:
      - missing_event_type
      - too_vague
      - too_specific
      - wrong_timeframe
      - static_content
      - article_request
      - multiple_event_types
      - too_short
      - too_long
      title: IssueType
      description: Specific categories of query issues
    QueryStatus:
      type: string
      enum:
      - good
      - needs_work
      - critical
      title: QueryStatus
      description: Query quality assessment levels
    Suggestion:
      properties:
        issue:
          $ref: '#/components/schemas/IssueType'
        message:
          type: string
          title: Message
          description: Clear, specific guidance
        example:
          anyOf:
          - type: string
          - type: 'null'
          title: Example
          description: Example of improved query
      type: object
      required:
      - issue
      - message
      title: Suggestion
      description: Actionable recommendation for query improvement
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
            - type: string
            - type: integer
          type: array
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
      type: object
      required:
      - loc
      - msg
      - type
      title: ValidationError
    UserJobResponseDto:
      properties:
        job_id:
          type: string
          format: uuid
          title: Job Id
          description: Job identifier.
        query:
          type: string
          title: Query
          description: Plain text query for this job.
        created_at:
          type: string
          format: date-time
          title: Created At
          description: Job creation timestamp.
        status:
          $ref: '#/components/schemas/PublicJobStatus'
          description: Current processing status of the job.
        user_key:
          type: string
          title: User Key
          description: Masked API key that created this job.
        sharing_info:
          anyOf:
          - $ref: '#/components/schemas/SharingInfoDto'
          - type: 'null'
          description: Present when this job was shared with the authenticated user.
            Omitted when the user owns the job.
        mode:
          $ref: '#/components/schemas/ProcessingMode'
          description: Processing mode used for this job.
          default: base
      type: object
      required:
      - job_id
      - query
      - created_at
      - status
      - user_key
      title: UserJob
    ConnectedDatasetDto:
      properties:
        id:
          type: string
          format: uuid
          title: Id
          description: Dataset identifier.
        name:
          type: string
          title: Name
          description: Dataset name as stored. Check is_deleted to determine whether
            the dataset is still active.
        is_deleted:
          type: boolean
          title: Is Deleted
          description: True when the dataset has been deleted.
          default: false
      type: object
      required:
      - id
      - name
      title: ConnectedDataset
    SharingInfoDto:
      properties:
        shared_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Shared At
        shared_by_user_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Shared By User Id
        permission:
          anyOf:
          - type: string
          - type: 'null'
          title: Permission
      type: object
      title: SharingInfo
    EnrichmentSchema:
      properties:
        name:
          type: string
          title: Name
          description: Machine-friendly field name (snake_case recommended).
          example: company_name
        description:
          type: string
          title: Description
          description: What information to extract from each article.
          example: The name of the company that is conducting the layoffs.
        type:
          $ref: '#/components/schemas/EnrichmentType'
          description: 'Data type for this enrichment field. Supported: text, number,
            date, option, url, company.'
          example: company
      type: object
      required:
      - name
      - description
      - type
      title: EnrichmentSchema
      description: A custom extraction field added to each validated article.
    ValidatorSchema:
      properties:
        name:
          type: string
          title: Name
          description: Machine-friendly field name (snake_case recommended).
          example: is_about_layoffs
        description:
          type: string
          title: Description
          description: Natural-language criterion evaluated against each article.
            Returns true if the article satisfies this criterion.
          example: The article is specifically about job layoffs or workforce reductions.
        type:
          type: string
          const: boolean
          title: Type
          description: Always 'boolean'. Only articles where all validators return
            true count as valid_records.
          default: boolean
      type: object
      required:
      - name
      - description
      title: ValidatorSchema
      description: A boolean classifier applied to every candidate article.
    ReferenceJob:
      properties:
        query:
          anyOf:
          - type: string
          - type: 'null'
          title: Query
          description: Plain text query from the reference job.
        context:
          anyOf:
          - type: string
          - type: 'null'
          title: Context
          description: Context provided with the reference job query.
      type: object
      title: ReferenceJob
    RunInfoDto:
      properties:
        first_run:
          anyOf:
          - type: string
          - type: 'null'
          title: First Run
          description: ISO 8601 timestamp of the first completed run.
        last_run:
          anyOf:
          - type: string
          - type: 'null'
          title: Last Run
          description: ISO 8601 timestamp of the most recent completed run.
      type: object
      title: RunInfo
    MonitorRecordDto:
      properties:
        record_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Record Id
          description: Unique cluster identifier for this record.
        record_title:
          anyOf:
          - type: string
          - type: 'null'
          title: Record Title
          description: Event or cluster title.
        enrichment:
          additionalProperties: true
          type: object
          title: Enrichment
          description: Custom enrichment fields extracted for this record. Keys match
            the enrichment names defined in the job. Values are job-specific. For
            'company' type enrichments, each value is a CompanyEnrichmentOutput object.
        enrichment_confidence:
          anyOf:
          - additionalProperties:
              type: number
            type: object
          - type: 'null'
          title: Enrichment Confidence
          description: Per-field confidence scores for enrichment values. Keys mirror
            the enrichment dict; values are floats in [0, 1].
        citations:
          anyOf:
          - items:
              $ref: '#/components/schemas/MonitorCitationDto'
            type: array
          - type: 'null'
          title: Citations
          description: Source articles included in this record, with job attribution.
        connected_entities:
          anyOf:
          - items:
              $ref: '#/components/schemas/ConnectedEntityDto'
            type: array
          - type: 'null'
          title: Connected Entities
          description: Entities from connected datasets that matched this record.
        added_on:
          anyOf:
          - type: string
          - type: 'null'
          title: Added On
          description: ISO 8601 timestamp when this record was first added.
        updated_on:
          anyOf:
          - type: string
          - type: 'null'
          title: Updated On
          description: ISO 8601 timestamp when this record was last updated.
      type: object
      title: MonitorRecord
    WebhookResponseDto:
      properties:
        id:
          type: string
          format: uuid
          title: Id
          description: Webhook identifier.
        name:
          type: string
          title: Name
          description: Webhook name.
        url:
          type: string
          title: Url
          description: Webhook endpoint URL.
        type:
          type: string
          title: Type
          description: Webhook target type.
        delivery_mode:
          type: string
          title: Delivery Mode
          description: Delivery mode for webhook payloads.
        method:
          type: string
          title: Method
          description: HTTP method used for delivery.
        headers:
          additionalProperties: true
          type: object
          title: Headers
          description: HTTP headers included in requests.
        params:
          additionalProperties: true
          type: object
          title: Params
          description: Query string parameters.
        auth:
          anyOf:
          - oneOf:
            - $ref: '#/components/schemas/BearerAuthResponseDto'
            - $ref: '#/components/schemas/ApiKeyAuthResponseDto'
            - $ref: '#/components/schemas/BasicAuthResponseDto'
            discriminator:
              propertyName: type
              mapping:
                api_key: '#/components/schemas/ApiKeyAuthResponseDto'
                basic: '#/components/schemas/BasicAuthResponseDto'
                bearer: '#/components/schemas/BearerAuthResponseDto'
          - type: 'null'
          title: Auth
          description: Authentication configuration for this webhook. Sensitive values
            (token, API key value, password) are masked as '***'. Present with type
            indicator when auth is configured; null when no auth is set.
        formatter_config:
          anyOf:
          - $ref: '#/components/schemas/FormatterConfigDto'
          - type: 'null'
          description: Formatter configuration.
        is_active:
          type: boolean
          title: Is Active
          description: True if the webhook is active; false otherwise.
        organization_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Organization Id
          description: Organization that owns this webhook.
        created_by_user_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Created By User Id
          description: ID of the user who created this webhook.
        created_at:
          anyOf:
          - type: string
          - type: 'null'
          title: Created At
          description: Timestamp of when the webhook was created.
        updated_at:
          anyOf:
          - type: string
          - type: 'null'
          title: Updated At
          description: Timestamp of when the webhook was last updated.
      type: object
      required:
      - id
      - name
      - url
      - type
      - delivery_mode
      - method
      - is_active
      title: WebhookDto
    AdditionalAttributesDto:
      properties:
        company_attributes:
          anyOf:
          - $ref: '#/components/schemas/CompanyAttributesDto'
          - type: 'null'
          description: Identifying attributes for a company entity.
      additionalProperties: true
      type: object
      title: AdditionalAttributes
    DateRangeDto:
      properties:
        start_date:
          anyOf:
          - type: string
          - type: 'null'
          title: Start Date
          description: Inclusive start of the article date range (ISO 8601).
        end_date:
          anyOf:
          - type: string
          - type: 'null'
          title: End Date
          description: Inclusive end of the article date range (ISO 8601).
      type: object
      title: DateRange
    ArticleRecordDto:
      properties:
        record_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Record Id
          description: Unique cluster identifier for this record.
        record_title:
          anyOf:
          - type: string
          - type: 'null'
          title: Record Title
          description: Event or cluster title.
        enrichment:
          additionalProperties: true
          type: object
          title: Enrichment
          description: Custom enrichment fields extracted for this record. Keys match
            the enrichment names defined in the job. Values are job-specific. For
            'company' type enrichments, each value is a CompanyEnrichmentOutput object.
        enrichment_confidence:
          anyOf:
          - additionalProperties:
              type: number
            type: object
          - type: 'null'
          title: Enrichment Confidence
          description: Per-field confidence scores for enrichment values. Keys mirror
            the enrichment dict; values are floats in [0, 1].
        citations:
          anyOf:
          - items:
              $ref: '#/components/schemas/CitationDto'
            type: array
          - type: 'null'
          title: Citations
          description: Source articles included in this record.
        connected_entities:
          anyOf:
          - items:
              $ref: '#/components/schemas/ConnectedEntityDto'
            type: array
          - type: 'null'
          title: Connected Entities
          description: Entities from connected datasets that matched this record.
            Present only when entity datasets are used.
      type: object
      title: Record
    PublicJobStatus:
      type: string
      enum:
      - submitted
      - analyzing
      - fetching
      - clustering
      - enriching
      - completed
      - failed
      title: PublicJobStatus
      description: 'Current job processing status. Jobs progress through stages: submitted
        → analyzing → fetching → clustering → enriching → completed. Poll /catchAll/status/{job_id}
        every 30-60 seconds until completed (typically 10-15 minutes).'
    WebhookStatusItemDto:
      properties:
        id:
          type: integer
          title: Id
        webhook_id:
          type: string
          format: uuid
          title: Webhook Id
        resource_type:
          $ref: '#/components/schemas/WebhookHistoryResourceType'
        resource_id:
          type: string
          format: uuid
          title: Resource Id
        additional_info:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Additional Info
        status_code:
          anyOf:
          - type: integer
          - type: 'null'
          title: Status Code
        attempt_number:
          type: integer
          title: Attempt Number
        timestamp:
          type: string
          format: date-time
          title: Timestamp
        delivery_status:
          $ref: '#/components/schemas/WebhookDeliveryStatus'
        error_message:
          anyOf:
          - type: string
          - type: 'null'
          title: Error Message
        warning_message:
          anyOf:
          - type: string
          - type: 'null'
          title: Warning Message
      type: object
      required:
      - id
      - webhook_id
      - resource_type
      - resource_id
      - attempt_number
      - timestamp
      - delivery_status
      title: WebhookStatusItemDto
    ProjectResponseDto:
      properties:
        project_id:
          type: string
          format: uuid
          title: Project Id
          description: Project identifier.
        name:
          type: string
          title: Name
          description: Project name.
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Project description.
        resources_count:
          type: integer
          title: Resources Count
          description: Number of resources assigned to this project.
          default: 0
        created_at:
          anyOf:
          - type: string
          - type: 'null'
          title: Created At
          description: Project creation timestamp.
        updated_at:
          anyOf:
          - type: string
          - type: 'null'
          title: Updated At
          description: Timestamp of the last update.
        sharing_info:
          anyOf:
          - $ref: '#/components/schemas/SharingInfoDto'
          - type: 'null'
          description: Present when this project was shared with the authenticated
            user. Omitted when the user owns the project.
      type: object
      required:
      - project_id
      - name
      title: ProjectResponseDto
    WebhookResourceMappingResponseDto:
      properties:
        id:
          type: string
          format: uuid
          title: Id
          description: Mapping identifier.
        webhook_id:
          type: string
          format: uuid
          title: Webhook Id
          description: Webhook identifier.
        resource_type:
          type: string
          title: Resource Type
          description: Type of the assigned resource.
        resource_id:
          type: string
          format: uuid
          title: Resource Id
          description: Resource identifier.
        assigned_at:
          anyOf:
          - type: string
          - type: 'null'
          title: Assigned At
          description: Timestamp of when the resource was assigned.
      type: object
      required:
      - id
      - webhook_id
      - resource_type
      - resource_id
      title: WebhookResourceMappingDto
    BearerAuthDto:
      properties:
        type:
          type: string
          const: bearer
          title: Type
          description: Authentication type.
        token:
          type: string
          title: Token
          description: Bearer token value sent in the Authorization header.
      type: object
      required:
      - type
      - token
      title: BearerAuth
    WebhookType:
      type: string
      enum:
      - generic
      - slack
      - teams
      - custom
      title: WebhookType
      description: Webhook target type. 'slack' and 'teams' send pre-formatted payloads;
        'generic' and 'custom' send the raw result payload.
    ApiKeyAuthDto:
      properties:
        type:
          type: string
          const: api_key
          title: Type
          description: Authentication type.
        header:
          type: string
          title: Header
          description: HTTP header name for the API key, e.g. 'X-API-Key'.
        value:
          type: string
          title: Value
          description: API key value.
      type: object
      required:
      - type
      - header
      - value
      title: ApiKeyAuth
    DeliveryMode:
      type: string
      enum:
      - full
      - per_record
      title: DeliveryMode
      description: Delivery mode for webhook payloads. 'full' delivers the entire
        result set in one call; 'per_record' sends one call per article.
    HttpMethod:
      type: string
      enum:
      - GET
      - POST
      - PUT
      - PATCH
      - DELETE
      title: HttpMethod
      description: HTTP method used for webhook delivery.
    FormatterConfigDto:
      properties:
        template:
          type: string
          title: Template
          description: Liquid template string rendered at dispatch time. Maximum 10
            KB.
          example: '{"event": "{{ event }}", "count": {{ records_count }}}'
        content_type:
          type: string
          title: Content Type
          description: 'Content-Type of the rendered output. Supported values: application/json,
            application/ld+json, text/html, text/plain, text/xml, application/xml,
            text/csv.'
          default: application/json
          example: application/json
      type: object
      required:
      - template
      title: FormatterConfig
    BasicAuthDto:
      properties:
        type:
          type: string
          const: basic
          title: Type
          description: Authentication type.
        username:
          type: string
          title: Username
          description: Basic auth username.
        password:
          type: string
          title: Password
          description: Basic auth password.
      type: object
      required:
      - type
      - username
      - password
      title: BasicAuth
    EntitySummaryDto:
      properties:
        id:
          type: string
          format: uuid
          title: Id
          description: Unique identifier of the entity.
        name:
          type: string
          title: Name
          description: Entity name.
        entity_type:
          type: string
          title: Entity Type
          description: The type of entity.
        status:
          type: string
          title: Status
          description: Processing status of the entity.
        description:
          anyOf:
          - type: string
          - type: 'null'
          title: Description
          description: Free-text description.
        external_entity_id:
          anyOf:
          - type: string
          - type: 'null'
          title: External Entity Id
          description: External identifier for this entity. Null when not set.
        attributes:
          anyOf:
          - $ref: '#/components/schemas/AdditionalAttributesDto'
          - type: 'null'
          description: Type-specific attributes.
      type: object
      required:
      - id
      - name
      - entity_type
      - status
      title: EntitySummary
    MonitorJobItemResponseDto:
      properties:
        job_id:
          type: string
          format: uuid
          title: Job Id
          description: Unique identifier for this job execution.
        start_date:
          anyOf:
          - type: string
          - type: 'null'
          title: Start Date
          description: Start of the data collection time window for this job execution.
        end_date:
          anyOf:
          - type: string
          - type: 'null'
          title: End Date
          description: End of the data collection time window for this job execution.
      type: object
      required:
      - job_id
      title: MonitorJobItem
    ResourceItemRequestDto:
      properties:
        resource_type:
          $ref: '#/components/schemas/ProjectResourceTypeEnum'
          description: Type of resource to add.
        resource_id:
          type: string
          format: uuid
          title: Resource Id
          description: ID of the resource to add.
      type: object
      required:
      - resource_type
      - resource_id
      title: ResourceItemDto
    EntityAssociationType:
      type: string
      enum:
      - mention
      - event_associated
      title: EntityAssociationType
      description: 'How an entity relates to an event: direct actor or merely mentioned.'
    ResourceResultResponseDto:
      properties:
        resource_type:
          type: string
          title: Resource Type
          description: Type of the resource.
        resource_id:
          type: string
          format: uuid
          title: Resource Id
          description: Resource identifier.
        success:
          type: boolean
          title: Success
          description: True if this resource was processed successfully.
        message:
          type: string
          title: Message
          description: Per-resource result message.
        already_exists:
          type: boolean
          title: Already Exists
          description: True if the resource is already assigned to this project.
          default: false
      type: object
      required:
      - resource_type
      - resource_id
      - success
      - message
      title: ResourceResultDto
    ResourceItemResponseDto:
      properties:
        resource_type:
          type: string
          title: Resource Type
          description: Type of the resource.
        resource_id:
          type: string
          format: uuid
          title: Resource Id
          description: Resource identifier.
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
          description: Resource display name.
        created_at:
          anyOf:
          - type: string
          - type: 'null'
          title: Created At
          description: Resource creation timestamp.
        metadata:
          additionalProperties: true
          type: object
          title: Metadata
          description: Additional resource-specific metadata.
      type: object
      required:
      - resource_type
      - resource_id
      title: ProjectResourceDto
    MonitorListItemResponseDto:
      properties:
        monitor_id:
          type: string
          format: uuid
          title: Monitor Id
          description: Monitor identifier.
        reference_job_id:
          type: string
          format: uuid
          title: Reference Job Id
          description: Job identifier used as a reference for this monitor.
        reference_job_query:
          type: string
          title: Reference Job Query
          description: Plain text query from the reference job.
        sharing_info:
          anyOf:
          - $ref: '#/components/schemas/SharingInfoDto'
          - type: 'null'
          description: Present when this monitor was shared with the authenticated
            user. Omitted when the user owns the monitor.
        enabled:
          type: boolean
          title: Enabled
          description: True if the monitor is currently active; false otherwise.
        schedule:
          anyOf:
          - type: string
          - type: 'null'
          title: Schedule
          description: Cron expression for monitor schedule.
        timezone:
          anyOf:
          - type: string
          - type: 'null'
          title: Timezone
          description: Timezone for schedule execution.
        created_at:
          anyOf:
          - type: string
          - type: 'null'
          title: Created At
          description: Timestamp of when the monitor was created.
        schedule_human_readable:
          anyOf:
          - type: string
          - type: 'null'
          title: Schedule Human Readable
          description: Monitor schedule in plain text format.
        user_key:
          anyOf:
          - type: string
          - type: 'null'
          title: User Key
          description: Masked API key associated with this monitor.
      type: object
      required:
      - monitor_id
      - reference_job_id
      - reference_job_query
      - enabled
      title: MonitorListItemDto
    CsvValidationReportDto:
      properties:
        total_rows:
          type: integer
          title: Total Rows
          description: Total number of data rows in the uploaded CSV excluding the
            header.
        valid_rows:
          type: integer
          title: Valid Rows
          description: Number of rows successfully processed into entities.
        skipped_count:
          type: integer
          title: Skipped Count
          description: Number of rows skipped due to validation errors.
        skipped_rows:
          items:
            $ref: '#/components/schemas/CsvSkippedRowDto'
          type: array
          title: Skipped Rows
          description: Details for each skipped row.
      type: object
      required:
      - total_rows
      - valid_rows
      - skipped_count
      - skipped_rows
      title: ValidationReport
    MonitorStatusEntryDto:
      properties:
        id:
          type: integer
          title: Id
          description: Status entry identifier.
        status:
          type: string
          title: Status
          description: Type of lifecycle event.
        created_at:
          type: string
          format: date-time
          title: Created At
          description: Timestamp of this event.
        additional_information:
          anyOf:
          - {}
          - type: 'null'
          title: Additional Information
          description: Event-specific metadata. Null for created, enable, and disable
            events.
      type: object
      required:
      - id
      - status
      - created_at
      title: MonitorStatusEntry
    DatasetStatusEntryDto:
      properties:
        status:
          type: string
          title: Status
          description: Dataset processing status.
        additional_information:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Additional Information
          description: Additional context about the status change.
        created_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Created At
          description: Timestamp of when this status was set.
      type: object
      required:
      - status
      title: DatasetStatusEntry
    JobStep:
      properties:
        status:
          $ref: '#/components/schemas/PublicJobStatus'
        order:
          type: integer
          title: Order
        completed:
          type: boolean
          title: Completed
          default: false
      type: object
      required:
      - status
      - order
      title: JobStep
    BillingFeatureWithUsage:
      properties:
        name:
          type: string
          title: Name
          description: Human-readable feature name.
        code:
          type: string
          title: Code
          description: Machine-readable feature identifier.
        value_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Value Type
          description: Data type of the feature value.
        value:
          anyOf:
          - {}
          - type: 'null'
          title: Value
          description: Feature limit. Type depends on value_type.
        current_usage:
          anyOf:
          - type: integer
          - type: number
          - type: 'null'
          title: Current Usage
          description: Current usage count for this feature.
      type: object
      required:
      - name
      - code
      title: PlanFeature
    EnrichmentType:
      type: string
      enum:
      - text
      - number
      - date
      - option
      - url
      - company
      title: EnrichmentType
      description: 'Canonical enrichment types: text, number, date, option, url, company.
        See Company enrichment docs for the company type data model.'
    ConnectedEntityDto:
      properties:
        entity_id:
          type: string
          format: uuid
          title: Entity Id
          description: ID of the matched entity from the connected dataset.
        name:
          type: string
          title: Name
          description: Entity name.
        type:
          type: string
          title: Type
          description: Entity type, e.g. 'company'.
        ed_score:
          anyOf:
          - type: number
          - type: 'null'
          title: Ed Score
          description: Entity detection confidence score.
        association_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Association Type
          description: 'How the entity relates to the event: ''event_associated''
            if direct actor, ''mention'' if merely referenced.'
        relation:
          anyOf:
          - type: string
          - type: 'null'
          title: Relation
          description: Sentence describing how this entity relates to the article.
        external_entity_id:
          anyOf:
          - type: string
          - type: 'null'
          title: External Entity Id
          description: External identifier for this entity. Null when not set.
        company:
          anyOf:
          - $ref: '#/components/schemas/CompanyAttributesDto'
          - type: 'null'
          description: Company-specific attributes. Present when type == 'company'.
      type: object
      required:
      - entity_id
      - name
      - type
      title: ConnectedEntity
    MonitorCitationDto:
      properties:
        type:
          anyOf:
          - type: string
          - type: 'null'
          title: Type
          description: Source type of the citation, e.g. 'news'.
        title:
          anyOf:
          - type: string
          - type: 'null'
          title: Title
          description: Title of the source document.
        link:
          anyOf:
          - type: string
          - type: 'null'
          format: uri
          title: Link
          description: URL to the source document.
        published_date:
          anyOf:
          - type: string
          - type: 'null'
          title: Published Date
          description: Publication date of the source document.
        id:
          anyOf:
          - type: string
          - type: 'null'
          title: Id
          description: Citation identifier.
        job_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Job Id
          description: Job identifier that found this citation.
        added_on:
          anyOf:
          - type: string
          - type: 'null'
          title: Added On
          description: ISO 8601 timestamp when this citation was added.
      type: object
      title: MonitorCitation
    BearerAuthResponseDto:
      properties:
        type:
          type: string
          const: bearer
          title: Type
        token:
          anyOf:
          - type: string
          - type: 'null'
          title: Token
          description: Masked bearer token.
      type: object
      required:
      - type
      title: BearerAuthResponse
    ApiKeyAuthResponseDto:
      properties:
        type:
          type: string
          const: api_key
          title: Type
        header:
          anyOf:
          - type: string
          - type: 'null'
          title: Header
          description: HTTP header name for the API key.
        value:
          anyOf:
          - type: string
          - type: 'null'
          title: Value
          description: Masked API key value.
      type: object
      required:
      - type
      title: ApiKeyAuthResponse
    BasicAuthResponseDto:
      properties:
        type:
          type: string
          const: basic
          title: Type
        username:
          anyOf:
          - type: string
          - type: 'null'
          title: Username
          description: Basic auth username.
        password:
          anyOf:
          - type: string
          - type: 'null'
          title: Password
          description: Masked password.
      type: object
      required:
      - type
      title: BasicAuthResponse
    CompanyAttributesDto:
      properties:
        alternative_names:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Alternative Names
          description: Alternative names, abbreviations, or aliases for the company.
        domain:
          anyOf:
          - type: string
          - type: 'null'
          title: Domain
          description: Company website domain without protocol or trailing slash.
            On create, the entity must have this or description set.
        key_persons:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Key Persons
          description: Names of key people associated with the company.
      type: object
      title: CompanyAttributes
    CitationDto:
      properties:
        type:
          anyOf:
          - type: string
          - type: 'null'
          title: Type
          description: Source type of the citation, e.g. 'news'.
        title:
          anyOf:
          - type: string
          - type: 'null'
          title: Title
          description: Title of the source document.
        link:
          anyOf:
          - type: string
          - type: 'null'
          format: uri
          title: Link
          description: URL to the source document.
        published_date:
          anyOf:
          - type: string
          - type: 'null'
          title: Published Date
          description: Publication date of the source document.
      type: object
      title: Citation
    WebhookHistoryResourceType:
      type: string
      enum:
      - job
      - monitor
      - monitor_group
      - test
      title: WebhookHistoryResourceType
      description: 'Resource types a webhook_status row can be recorded against.


        A superset of MappableResourceType: real deliveries are always tied to an

        attachable resource (job/monitor/monitor_group), but a manual test delivery

        (POST /webhooks/{id}/test) isn''t tied to any real resource, so it is recorded

        against the synthetic `test` type instead. Kept separate from

        MappableResourceType so `test` can never be accepted where an attachable

        resource type is expected (e.g. assigning a webhook to a resource).'
    WebhookDeliveryStatus:
      type: string
      enum:
      - SUCCESS
      - FAILED
      title: WebhookDeliveryStatus
      description: Webhook delivery outcome status.
    CsvSkippedRowDto:
      properties:
        row:
          type: integer
          title: Row
          description: 1-based row number in the CSV file including the header row.
        reason:
          type: string
          title: Reason
          description: Human-readable explanation of why the row was skipped.
      type: object
      required:
      - row
      - reason
      title: SkippedRow
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: X-API-Key
security:
- ApiKeyAuth: []
